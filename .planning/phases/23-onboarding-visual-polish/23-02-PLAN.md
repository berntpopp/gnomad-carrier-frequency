---
phase: 23-onboarding-visual-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/AppBar.vue
autonomous: true

must_haves:
  truths:
    - "On mobile (xs breakpoint), the app title 'gCFCalc' is hidden"
    - "On mobile Steps 2-4, a gene context chip shows the selected gene symbol and gnomAD version"
    - "Tapping the context chip navigates back to Step 1"
    - "On desktop (sm+), the title shows normally and no context chip appears"
    - "On Step 1 (mobile), no context chip is shown (gene selection happens here)"
  artifacts:
    - path: "src/components/AppBar.vue"
      provides: "Responsive AppBar with xs title hiding and gene context chip"
      contains: "useDisplay"
  key_links:
    - from: "src/components/AppBar.vue"
      to: "src/composables/useWizard.ts"
      via: "state.currentStep and state.gene for chip visibility"
      pattern: "useWizard"
    - from: "src/components/AppBar.vue"
      to: "src/api/client.ts"
      via: "useGnomadVersion for version display in chip"
      pattern: "useGnomadVersion"
---

<objective>
Hide the app title on mobile (xs breakpoint) and add a persistent gene context chip in the AppBar showing the selected gene and gnomAD version on Steps 2-4.

Purpose: Maximize vertical space on small screens while maintaining contextual awareness of which gene and gnomAD version the user is working with.
Output: Modified AppBar.vue with responsive title visibility and gene context chip.
</objective>

<execution_context>
@C:\Users\bernt\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\bernt\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-onboarding-visual-polish/23-CONTEXT.md
@.planning/phases/23-onboarding-visual-polish/23-RESEARCH.md

Key source files:
@src/components/AppBar.vue
@src/composables/useWizard.ts
@src/api/client.ts
@src/components/wizard/WizardStepper.vue (reference: existing useDisplay usage)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add responsive title hiding and gene context chip to AppBar</name>
  <files>src/components/AppBar.vue</files>
  <action>
**Import new dependencies in `<script setup>`:**

1. Import `useDisplay` from `'vuetify'`.
2. Import `useWizard` from `'@/composables'`.
3. Import `useGnomadVersion` from `'@/api'`.
4. Destructure: `const { xs } = useDisplay();`
5. Destructure: `const { state, goToStep } = useWizard();`
6. Destructure: `const { version } = useGnomadVersion();`

**Hide title on xs breakpoint:**

1. Wrap the existing `v-tooltip` + `v-app-bar-title` block with `v-if="!xs"`. Add `v-if="!xs"` to the outer `v-tooltip` element that wraps the `v-app-bar-title`. This hides both the tooltip and the title on mobile.

**Add gene context chip (mobile only, Steps 2-4):**

1. After the `v-tooltip` block (title), before `<OfflineIndicator>`, add a `v-chip`:
   ```
   <v-chip
     v-if="xs && state.currentStep > 1 && state.gene"
     size="small"
     color="primary"
     variant="tonal"
     class="ml-2"
     data-testid="gene-context-chip"
     @click="goToStep(1)"
   >
     {{ state.gene.symbol }} &middot; {{ version }}
   </v-chip>
   ```
2. The chip uses interpunct separator (`&middot;` or `\u00B7`) between gene symbol and version.
3. Clicking the chip calls `goToStep(1)` to navigate back to Step 1 for gene re-selection.
4. The chip is ONLY visible when: `xs` (mobile), `currentStep > 1` (not on gene selection step), and `state.gene` (a gene is selected).

**Ensure OfflineIndicator stays visible on all breakpoints.** It's already placed after the title — on mobile when title is hidden, the chip takes the title's position and OfflineIndicator follows.
  </action>
  <verify>
Run `bun run typecheck` — no type errors. Run `bun run build` — build succeeds. Verify AppBar.vue imports `useDisplay`, `useWizard`, `useGnomadVersion`. Verify the `v-chip` has `v-if="xs && state.currentStep > 1 && state.gene"`. Verify the title tooltip has `v-if="!xs"`.
  </verify>
  <done>
AppBar hides title on xs breakpoint. Gene context chip appears on mobile Steps 2-4 showing "GENE · vX.X" format. Tapping chip navigates to Step 1. Desktop layout unchanged.
  </done>
</task>

</tasks>

<verification>
1. `bun run typecheck` passes
2. `bun run build` succeeds
3. `bun run lint` passes (or only pre-existing warnings)
4. Manual verification: In browser dev tools, toggle to mobile viewport. On Step 1, title is hidden, no chip. Select a gene and advance to Step 2 — chip appears with gene name and version. Click chip — returns to Step 1. On desktop viewport — title shows, no chip appears on any step.
</verification>

<success_criteria>
- AppBar title hidden on xs breakpoint
- Gene context chip visible on mobile Steps 2-4 with "GENE · version" format
- Chip click navigates to Step 1
- Desktop layout unchanged (title visible, no chip)
- No chip on Step 1 (mobile or desktop)
</success_criteria>

<output>
After completion, create `.planning/phases/23-onboarding-visual-polish/23-02-SUMMARY.md`
</output>
