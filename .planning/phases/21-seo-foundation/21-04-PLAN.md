---
phase: 21-seo-foundation
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/AppFooter.vue
  - docs/use-cases/carrier-screening.md
  - docs/use-cases/clinical-letter.md
  - docs/guide/getting-started.md
  - docs/guide/index.md
  - docs/reference/methodology.md
autonomous: true

must_haves:
  truths:
    - "App footer contains a 'Docs' icon button linking to the documentation site"
    - "Docs pages include contextual 'Open Calculator' CTA links that deep-link to the app with relevant parameters"
    - "Navigating from app footer to docs and from docs back to calculator works without broken links"
  artifacts:
    - path: "src/components/AppFooter.vue"
      provides: "Docs link button in footer-primary row"
      contains: "mdi-book-open"
    - path: "docs/use-cases/carrier-screening.md"
      provides: "CTA link to calculator with CFTR deep link"
      contains: "gnomad-carrier-frequency.kidney-genetics.org"
    - path: "docs/guide/getting-started.md"
      provides: "CTA link to calculator"
      contains: "gnomad-carrier-frequency.kidney-genetics.org"
  key_links:
    - from: "src/components/AppFooter.vue"
      to: "/docs/"
      via: "href on v-btn"
      pattern: "href.*docs"
    - from: "docs/use-cases/carrier-screening.md"
      to: "app root"
      via: "markdown link with query params"
      pattern: "gnomad-carrier-frequency\\.kidney-genetics\\.org/\\?gene=CFTR"
---

<objective>
Add cross-linking between the app and docs site in both directions.

Purpose: Search engines use cross-links to understand site structure and pass authority between pages. Users navigating from app to docs and back should have clear, working paths. Deep-links from docs to the calculator with pre-filled parameters reduce friction.

Output: Docs icon in app footer, contextual "Open Calculator" CTA links in docs pages with deep-link parameters where relevant.
</objective>

<execution_context>
@C:\Users\bernt\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\bernt\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-seo-foundation/21-CONTEXT.md
@.planning/phases/21-seo-foundation/21-RESEARCH.md
@src/components/AppFooter.vue
@docs/use-cases/carrier-screening.md
@docs/use-cases/clinical-letter.md
@docs/guide/getting-started.md
@docs/guide/index.md
@docs/reference/methodology.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Docs link to AppFooter</name>
  <files>src/components/AppFooter.vue</files>
  <action>
Add a "Documentation" icon button to the app footer (SEO-10). Place it in the `footer-primary` row, between the Version link and the Disclaimer button (so the order becomes: GitHub, Version, **Docs**, Disclaimer).

Follow the existing footer button pattern (v-tooltip wrapping v-btn with icon). Use `mdi-book-open-outline` icon.

```vue
<!-- Documentation -->
<v-tooltip
  text="Documentation"
  location="top"
  aria-label="Documentation"
>
  <template #activator="{ props }">
    <v-btn
      v-bind="props"
      icon
      variant="text"
      size="small"
      href="/docs/"
      target="_blank"
      rel="noopener noreferrer"
      aria-label="Open documentation"
    >
      <v-icon size="small">
        mdi-book-open-outline
      </v-icon>
    </v-btn>
  </template>
</v-tooltip>
```

Use a relative path `/docs/` (not absolute domain URL) since the docs are hosted at the same domain. The `target="_blank"` opens docs in a new tab so users don't lose their calculator state.

Also add a Docs item to the mobile overflow menu (the v-menu with `d-sm-none`). Add it after the "View Logs" item:
```vue
<v-list-item
  prepend-icon="mdi-book-open-outline"
  title="Documentation"
  href="/docs/"
  target="_blank"
  rel="noopener noreferrer"
/>
```
  </action>
  <verify>
1. `bun run build` succeeds
2. `bun run lint` passes (no lint errors in AppFooter.vue)
3. In the template, `mdi-book-open-outline` icon is present
4. Both desktop (footer-primary) and mobile (v-menu v-list) have the Docs link
5. href points to `/docs/` with `target="_blank"`
  </verify>
  <done>App footer has a Docs icon button visible on all screen sizes, linking to /docs/ in a new tab</done>
</task>

<task type="auto">
  <name>Task 2: Add calculator CTA links to docs pages</name>
  <files>docs/use-cases/carrier-screening.md, docs/use-cases/clinical-letter.md, docs/guide/getting-started.md, docs/guide/index.md, docs/reference/methodology.md</files>
  <action>
Add contextual "Open Calculator" CTA links to docs pages (SEO-12). Each page gets a CTA appropriate to its content. Deep-links use URL query parameters recognized by `useUrlState`: `gene`, `step`, `status`, `source`.

**1. `docs/use-cases/carrier-screening.md`** -- Add at the bottom, before "See Also":

```markdown
## Try It Yourself

Ready to calculate carrier frequency? Open the calculator pre-loaded with CFTR data:

[Try with CFTR](https://gnomad-carrier-frequency.kidney-genetics.org/?gene=CFTR){target="_blank" rel="noopener"}

Or start fresh with any gene: [Open Calculator](https://gnomad-carrier-frequency.kidney-genetics.org/){target="_blank" rel="noopener"}
```

**2. `docs/use-cases/clinical-letter.md`** -- Add at the bottom (before any "See Also" if present):

```markdown
## Try It Yourself

Generate clinical documentation text for your gene of interest:

[Open Calculator](https://gnomad-carrier-frequency.kidney-genetics.org/){target="_blank" rel="noopener"}
```

**3. `docs/guide/getting-started.md`** -- A "Launch Calculator" link already exists in the tip box at the top. Add a reinforcing CTA at the very bottom of the page:

```markdown
## Start Calculating

You now know the complete workflow. Open the calculator and try it with your gene of interest:

[Open Calculator](https://gnomad-carrier-frequency.kidney-genetics.org/){target="_blank" rel="noopener"}
```

**4. `docs/guide/index.md`** -- Add a CTA after the introduction content:

```markdown
## Get Started

[Open Calculator](https://gnomad-carrier-frequency.kidney-genetics.org/){target="_blank" rel="noopener"} | [Getting Started Guide](./getting-started)
```

**5. `docs/reference/methodology.md`** -- Add at the bottom:

```markdown
## Calculate Now

Apply these methods with real gnomAD data:

[Open Calculator](https://gnomad-carrier-frequency.kidney-genetics.org/){target="_blank" rel="noopener"}
```

**Note on link attributes**: VitePress supports `{target="_blank"}` attribute syntax on markdown links. If VitePress does not support this syntax in the current version, use plain markdown links without attributes -- VitePress already opens external links (full URLs) in new tabs by default. Test with `bun run docs:dev` after adding the links.

**Important**: Do NOT modify the existing content of these pages. Only ADD new sections at the bottom (before "See Also" sections where they exist). Preserve all existing headings, text, screenshots, and tip boxes.
  </action>
  <verify>
1. `bun run docs:build` succeeds (no markdown parsing errors)
2. Each of the 5 modified docs pages has at least one link to `https://gnomad-carrier-frequency.kidney-genetics.org/`
3. carrier-screening.md has a deep link with `?gene=CFTR`
4. `bun run docs:dev` -- manually verify links render correctly and navigate to the calculator
5. No existing content was removed or modified (only additions)
  </verify>
  <done>Five docs pages have contextual CTA links to the calculator, with deep-linking where relevant (CFTR for carrier screening). Links open calculator in new tab.</done>
</task>

</tasks>

<verification>
1. `bun run build` succeeds (app with footer change)
2. `bun run lint` passes
3. `bun run docs:build` succeeds (docs with CTA links)
4. AppFooter has mdi-book-open-outline icon linking to /docs/
5. 5 docs pages have calculator CTA links
6. carrier-screening.md includes ?gene=CFTR deep link
</verification>

<success_criteria>
- App footer has Documentation icon button visible on desktop and mobile
- At least 5 docs pages have "Open Calculator" CTA links
- carrier-screening.md deep-links to calculator with CFTR pre-filled
- All links use the correct production URL
- Bidirectional navigation (app to docs, docs to app) works
- Build and docs:build both pass
</success_criteria>

<output>
After completion, create `.planning/phases/21-seo-foundation/21-04-SUMMARY.md`
</output>
