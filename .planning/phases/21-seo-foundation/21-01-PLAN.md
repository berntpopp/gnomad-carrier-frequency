---
phase: 21-seo-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
autonomous: true

must_haves:
  truths:
    - "Viewing page source shows 500+ words of meaningful content inside `<div id=\"app\">` before JavaScript executes"
    - "Static HTML seed displays as a styled landing page while JavaScript loads, then Vue replaces it on mount"
    - "Page has canonical URL, robots directive, preconnect hints, and noscript fallback"
    - "Title tag leads with 'Carrier Frequency Calculator' and meta description includes differentiators"
    - "Static HTML includes mini-nav header with links to Calculator, Docs, FAQ, About and a mini-FAQ section (3-5 questions)"
  artifacts:
    - path: "index.html"
      provides: "Static HTML seed content, meta tags, canonical, robots, preconnect, noscript"
      contains: "seed-header"
      min_lines: 150
  key_links:
    - from: "index.html static nav"
      to: "/docs/"
      via: "anchor href"
      pattern: "href=\"/docs/\""
    - from: "index.html"
      to: "gnomad.broadinstitute.org"
      via: "preconnect link"
      pattern: "rel=\"preconnect\".*gnomad\\.broadinstitute\\.org"
---

<objective>
Create static HTML seed content inside `<div id="app">` in index.html and update all `<head>` meta tags for SEO.

Purpose: Google and social crawlers see meaningful content (500+ words) before JavaScript executes. The seed content serves as a styled landing page during JS load, then Vue replaces it on mount. Meta tags ensure proper indexing directives.

Output: Updated index.html with static seed content in body, optimized title/description, canonical URL, robots directive, preconnect hints, and noscript fallback.
</objective>

<execution_context>
@C:\Users\bernt\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\bernt\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-seo-foundation/21-CONTEXT.md
@.planning/phases/21-seo-foundation/21-RESEARCH.md
@index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update index.html head meta tags</name>
  <files>index.html</files>
  <action>
Update the `<head>` section of index.html with the following changes:

1. **Title tag** (SOP-01): Change from `gnomAD Carrier Frequency Calculator` to `Carrier Frequency Calculator | gnomAD Population Data | Free Clinical Tool`. Lead with the primary search term "Carrier Frequency Calculator" for SEO.

2. **Meta description** (SOP-02): Update to include differentiators. Keep under 155 characters. Include "free", "real population data", "multiple ancestries". Example: `Free carrier frequency calculator using gnomAD population data across multiple ancestries. Calculate recurrence risk for autosomal recessive conditions and generate clinical documentation.`

3. **Canonical URL** (SEO-02): Add `<link rel="canonical" href="https://gnomad-carrier-frequency.kidney-genetics.org/" />` with absolute URL including trailing slash.

4. **Robots directive** (SEO-03): Add `<meta name="robots" content="index, follow">`.

5. **Preconnect hints** (SOP-05): Add preconnect and dns-prefetch for gnomAD API:
   ```html
   <link rel="preconnect" href="https://gnomad.broadinstitute.org" />
   <link rel="dns-prefetch" href="https://gnomad.broadinstitute.org" />
   ```

6. **Inline style block**: Add a `<style>` block for seed content styling. Use `seed-` prefixed class names to avoid Vuetify conflicts. Style the seed as a professional landing page matching the app's `#a09588` brand color palette. Include styles for: `.seed-header`, `.seed-nav`, `.seed-main`, `.seed-h1`, `.seed-subtitle`, `.seed-features`, `.seed-faq-q`, `.seed-faq-a`, `.seed-cta`, `.seed-footer`. Use `font-family: system-ui, -apple-system, BlinkMacSystemFont, sans-serif`. Max-width 900px for main content. Responsive (single column on mobile).

Do NOT change the existing OG/Twitter meta tags or JSON-LD structured data yet -- those are handled in Plan 03.
  </action>
  <verify>
Open index.html and verify:
- Title starts with "Carrier Frequency"
- `<link rel="canonical">` present with absolute URL and trailing slash
- `<meta name="robots">` present
- Preconnect links present for gnomad.broadinstitute.org
- `<style>` block with seed-* classes present
  </verify>
  <done>Head section has optimized title, meta description with differentiators, canonical URL, robots directive, preconnect hints, and seed CSS styles</done>
</task>

<task type="auto">
  <name>Task 2: Add static HTML seed content and noscript fallback</name>
  <files>index.html</files>
  <action>
Replace the empty `<div id="app"></div>` with rich static HTML seed content inside `<div id="app">`. Vue 3's `createApp().mount('#app')` replaces the innerHTML on mount -- this is intended behavior. The seed content is visible to crawlers and users before JavaScript loads.

**Content structure (must reach 500+ words):**

1. **Header** (`.seed-header`): Mini-nav with logo text "gCFCalc" on left, navigation links on right: Calculator (href="/"), Documentation (href="/docs/"), FAQ (href="#faq"), About (href="/docs/about/"). Use the `seed-nav` class for link styling.

2. **Main content** (`.seed-main`):
   a. **H1**: "gnomAD Carrier Frequency Calculator"
   b. **Subtitle paragraph**: Accessible expert tone. Describe what the tool does AND who it's for. Mention genetic counselors, clinical documentation, population-specific frequencies, gnomAD data. ~80-100 words.
   c. **Feature highlights section** (~150 words across 4-6 feature items): Each feature has a bold title and 1-2 sentence description. Cover: (1) Real population data from gnomAD, (2) Multiple ancestries and founder effect detection, (3) Automated variant filtering (LoF HC + ClinVar), (4) Clinical text generation (German/English), (5) Recurrence risk calculation, (6) Works offline as installable PWA. Use `<ul>` or `<dl>` markup.
   d. **How It Works section** (~80 words): Brief 4-step description matching the wizard: Search gene -> Select status -> Review variants -> Get results. Use an ordered list.
   e. **Mini-FAQ section** (id="faq", ~200 words across 3-5 Q&As):
      - "How do I calculate carrier frequency from gnomAD data?"
      - "What populations are available?"
      - "Is this tool free to use?"
      - "Can I use these results in clinical practice?"
      - Optional: "What is carrier frequency?" (if needed for word count)
      Use `.seed-faq-q` for questions (use `<h3>` tags) and `.seed-faq-a` for answers.
   f. **CTA link**: "Open Calculator" button linking to "/" with `.seed-cta` class.

3. **Footer** (`.seed-footer`): "For Research Use Only" disclaimer text. One line.

**Tone**: Accessible expert -- accurate medical content but approachable, like a knowledgeable colleague explaining the tool. Balance technical accuracy with readability.

**After the closing `</div>` of `#app`, add:**

4. **Noscript fallback** (SEO-08): Place `<noscript>` tag OUTSIDE `<div id="app">` with a brief message: "This application requires JavaScript to run. Please enable JavaScript in your browser to use the gnomAD Carrier Frequency Calculator." Include a link to the docs site which works without JS (VitePress generates static HTML).

**Word count verification**: After writing, count the words in the seed content. Must be 500+ words. The FAQ answers should be substantive (40-60 words each) to reach the target.
  </action>
  <verify>
1. Count words inside `<div id="app">`: should be 500+ (use a word count tool or manually estimate)
2. Verify H1 tag contains "gnomAD Carrier Frequency Calculator"
3. Verify static nav links: /, /docs/, #faq, /docs/about/
4. Verify FAQ section has 3-5 Q&A pairs with h3 questions
5. Verify `<noscript>` tag is outside `<div id="app">`
6. Run `bun run build` to confirm the build succeeds with the new content
7. Run `bun run dev` and view source -- seed content should be visible in HTML source
  </verify>
  <done>
Static HTML seed content with 500+ words is inside `<div id="app">`, includes H1, features, FAQ, nav links to docs. Noscript fallback is outside the app div. Build passes. Seed displays as styled landing page before Vue mounts.
  </done>
</task>

</tasks>

<verification>
1. `bun run build` succeeds with no errors
2. View the built `dist/index.html` -- seed content visible in source
3. `<link rel="canonical">` points to `https://gnomad-carrier-frequency.kidney-genetics.org/`
4. `<meta name="robots" content="index, follow">` present
5. Title starts with "Carrier Frequency Calculator"
6. Meta description under 160 characters, contains "free" and "gnomAD"
7. Preconnect for gnomad.broadinstitute.org present
8. Word count of seed content >= 500
9. `<noscript>` present outside `#app`
</verification>

<success_criteria>
- index.html contains 500+ words of static HTML seed content inside `<div id="app">` that displays as a styled landing page
- Head meta tags include canonical URL, robots directive, optimized title/description, and preconnect hints
- Noscript fallback exists outside the app div
- `bun run build` passes
- Vue app still mounts correctly and replaces seed content
</success_criteria>

<output>
After completion, create `.planning/phases/21-seo-foundation/21-01-SUMMARY.md`
</output>
