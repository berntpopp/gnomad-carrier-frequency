---
phase: 21-seo-foundation
plan: 03
type: execute
wave: 2
depends_on: ["21-01", "21-02"]
files_modified:
  - index.html
autonomous: true

must_haves:
  truths:
    - "JSON-LD structured data contains WebApplication with updated version, dateModified, and screenshot fields"
    - "JSON-LD contains two FAQPage objects: one inLanguage 'en' with 8-10 questions, one inLanguage 'de' with 8-10 questions"
    - "OG and Twitter meta tags reference absolute HTTPS URL to og-image.png (not SVG)"
    - "FAQ questions cover methodology, limitations, privacy, and core carrier frequency concepts in both languages"
  artifacts:
    - path: "index.html"
      provides: "Expanded structured data and corrected OG meta tags"
      contains: "FAQPage"
  key_links:
    - from: "index.html og:image meta"
      to: "public/og-image.png"
      via: "absolute URL"
      pattern: "og:image.*https://gnomad-carrier-frequency\\.kidney-genetics\\.org/og-image\\.png"
    - from: "index.html JSON-LD"
      to: "schema.org"
      via: "application/ld+json script"
      pattern: "application/ld\\+json"
---

<objective>
Expand JSON-LD structured data with bilingual FAQPage schema and update OG/Twitter meta tags to reference the PNG image.

Purpose: Expanded FAQ structured data improves featured snippet eligibility and helps Google understand page content in both English and German. Correcting OG image from SVG to PNG fixes social sharing previews.

Output: Updated index.html with expanded @graph containing WebApplication + 2 FAQPage objects (en/de), and corrected OG/Twitter meta tags pointing to og-image.png via absolute URL.
</objective>

<execution_context>
@C:\Users\bernt\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\bernt\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-seo-foundation/21-CONTEXT.md
@.planning/phases/21-seo-foundation/21-RESEARCH.md
@.planning/phases/21-seo-foundation/21-01-SUMMARY.md
@.planning/phases/21-seo-foundation/21-02-SUMMARY.md
@index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update OG and Twitter meta tags to PNG with absolute URLs</name>
  <files>index.html</files>
  <action>
In the `<head>` section of index.html, update ALL Open Graph and Twitter Card meta tags to reference the PNG OG image with absolute HTTPS URLs (SEO-07).

Replace the existing OG tags with:
```html
<!-- Open Graph tags -->
<meta property="og:type" content="website" />
<meta property="og:title" content="gnomAD Carrier Frequency Calculator" />
<meta property="og:description" content="Free carrier frequency calculator using real gnomAD population data across multiple ancestries. Calculate recurrence risk and generate clinical documentation." />
<meta property="og:image" content="https://gnomad-carrier-frequency.kidney-genetics.org/og-image.png" />
<meta property="og:image:type" content="image/png" />
<meta property="og:image:width" content="1200" />
<meta property="og:image:height" content="630" />
<meta property="og:image:alt" content="gnomAD Carrier Frequency Calculator showing carrier frequency results for CFTR gene across multiple populations" />
<meta property="og:url" content="https://gnomad-carrier-frequency.kidney-genetics.org/" />
<meta property="og:site_name" content="gnomAD Carrier Frequency Calculator" />
```

Replace the existing Twitter tags with:
```html
<!-- Twitter Card tags -->
<meta name="twitter:card" content="summary_large_image" />
<meta name="twitter:title" content="gnomAD Carrier Frequency Calculator" />
<meta name="twitter:description" content="Free carrier frequency calculator using real gnomAD population data across multiple ancestries. Calculate recurrence risk and generate clinical documentation." />
<meta name="twitter:image" content="https://gnomad-carrier-frequency.kidney-genetics.org/og-image.png" />
<meta name="twitter:image:alt" content="gnomAD Carrier Frequency Calculator showing carrier frequency results" />
```

Key changes from current state:
- `./og-image.svg` -> `https://gnomad-carrier-frequency.kidney-genetics.org/og-image.png` (absolute URL, PNG format)
- Added `og:image:type`, `og:image:alt`, `og:site_name`
- Added `twitter:image:alt`
- OG description kept under 200 characters (Facebook/LinkedIn limit), Twitter description under 155 characters -- use same text for both, keeping it under 155 chars
  </action>
  <verify>
Grep index.html for `og-image.svg` -- should return zero matches (all replaced with .png)
Grep index.html for `og-image.png` -- should match absolute URLs in both OG and Twitter tags
  </verify>
  <done>OG and Twitter meta tags reference absolute HTTPS URL to og-image.png, no SVG references remain</done>
</task>

<task type="auto">
  <name>Task 2: Expand JSON-LD structured data with bilingual FAQPage</name>
  <files>index.html</files>
  <action>
Replace the entire `<script type="application/ld+json">` block in index.html `<head>` with an expanded version containing:

**1. WebApplication** (SOP-03): Update the existing WebApplication node:
- `softwareVersion`: Read from package.json (currently "1.3.0") -- hardcode "1.3.0" in the JSON-LD
- Add `dateModified`: "2026-02-23" (current date, update as appropriate)
- Add `screenshot`: "https://gnomad-carrier-frequency.kidney-genetics.org/og-image.png"
- Keep all existing fields (name, alternateName, url, description, applicationCategory, applicationSubCategory, operatingSystem, browserRequirements, offers, author, featureList)
- Ensure `applicationCategory` is `"HealthApplication"` (not MedicalWebPage -- per context decision)

**2. English FAQPage** (SOP-04, `inLanguage: "en"`): 8-10 questions covering:
- Methodology: "How do I calculate carrier frequency from gnomAD data?", "How does the Hardy-Weinberg equation work for carrier frequency?"
- Core concepts: "What is carrier frequency?", "What is the difference between carrier frequency and disease prevalence?", "How is recurrence risk calculated for autosomal recessive conditions?"
- Tool usage: "Which gnomAD population should I select?", "Can I exclude specific variants from the calculation?"
- Limitations/privacy: "Can I use these results for clinical decisions?", "Is my patient data stored or transmitted?", "What are the limitations of this calculator?"

Each answer should be 60-120 words, technically accurate, accessible expert tone. Cover the core topics that genetic counselors might search for.

**3. German FAQPage** (SOP-04, `inLanguage: "de"`): 8-10 questions that are NOT direct translations but target German-language search queries a German-speaking clinician would use:
- "Wie berechne ich die Heterozygotenfrequenz aus gnomAD-Daten?"
- "Was ist die Heterozygotenfrequenz (Carrier Frequency)?"
- "Wie wird das Wiederholungsrisiko bei autosomal-rezessiven Erkrankungen berechnet?"
- "Welche Population sollte ich auswahlen?"
- "Kann ich bestimmte Varianten von der Berechnung ausschliessen?"
- "Kann ich die Ergebnisse fur die klinische Beratung verwenden?"
- "Werden Patientendaten gespeichert oder ubertragen?"
- "Was sind die Grenzen dieses Rechners?"

Use proper German umlauts (a-umlaut, o-umlaut, u-umlaut, sz). Answers should be natural German, not machine-translated. Cover methodology, privacy (browser-only, no data transmission), and clinical use disclaimers.

**Structure**: Use `@graph` array with three objects: WebApplication, FAQPage (en), FAQPage (de). Ensure valid JSON (no trailing commas, proper escaping of special characters in text).

**Important**: The JSON-LD must be valid JSON. After writing, mentally parse it. Common errors: missing commas between array items, unescaped quotes in answer text, trailing commas.
  </action>
  <verify>
1. Validate JSON-LD is syntactically valid JSON (no parse errors)
2. Count FAQPage objects in @graph -- should be exactly 2
3. English FAQPage has 8-10 Question items
4. German FAQPage has 8-10 Question items
5. WebApplication has softwareVersion, dateModified, screenshot fields
6. `bun run build` succeeds
7. Copy the JSON-LD and paste into Google's Rich Results Test (https://search.google.com/test/rich-results) -- should validate without errors
  </verify>
  <done>JSON-LD @graph contains WebApplication with version/date/screenshot, English FAQPage with 8-10 Q&As, German FAQPage with 8-10 Q&As. All valid JSON, all technically accurate content.</done>
</task>

</tasks>

<verification>
1. `bun run build` succeeds
2. No references to `og-image.svg` remain in OG/Twitter tags (SVG file can stay in public/ as source)
3. All OG image URLs are absolute HTTPS
4. JSON-LD contains exactly 3 objects in @graph: WebApplication, FAQPage (en), FAQPage (de)
5. English FAQ has 8+ questions, German FAQ has 8+ questions
6. WebApplication has softwareVersion "1.3.0" and dateModified
</verification>

<success_criteria>
- OG and Twitter meta tags use absolute HTTPS URL to og-image.png
- JSON-LD structured data has WebApplication with version, dateModified, screenshot
- Two FAQPage objects exist: English (8-10 Q&As) and German (8-10 Q&As)
- FAQ content covers methodology, limitations, privacy, and core concepts
- German FAQ uses natural German with proper umlauts, targeting German search queries
- Build passes, JSON-LD is valid
</success_criteria>

<output>
After completion, create `.planning/phases/21-seo-foundation/21-03-SUMMARY.md`
</output>
