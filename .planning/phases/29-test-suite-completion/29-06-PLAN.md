---
phase: 29-test-suite-completion
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/e2e/history-restore.spec.ts
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "E2E test completes CFTR wizard to Step 4, opens HistoryDrawer, clicks saved entry, and verifies state restores to Step 4 with CFTR results"
  artifacts:
    - path: "apps/web/e2e/history-restore.spec.ts"
      provides: "History restore E2E test"
      min_lines: 80
  key_links:
    - from: "apps/web/e2e/history-restore.spec.ts"
      to: "apps/web/src/components/HistoryDrawer.vue"
      via: "data-testid selectors"
      pattern: "history-drawer|history-entry|footer-history-btn"
---

<objective>
Add the missing history restore E2E test to close Gap 1 from VERIFICATION.md.

Purpose: ROADMAP criterion 2 requires Playwright E2E tests that "validate URL sharing AND history restore." URL sharing is tested in url-sharing.spec.ts but no E2E exercises the HistoryDrawer restore flow. This plan adds that test.

Output: A new Playwright spec file `apps/web/e2e/history-restore.spec.ts` that proves the full history restore user flow works end-to-end.
</objective>

<execution_context>
@C:\Users\bernt\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\bernt\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-test-suite-completion/29-04-SUMMARY.md
@apps/web/e2e/url-sharing.spec.ts
@apps/web/e2e/cftr-wizard.spec.ts
@apps/web/e2e/fixtures/gnomad-responses.ts
@apps/web/src/components/HistoryDrawer.vue
@apps/web/src/components/HistoryPanel.vue
@apps/web/src/components/AppBar.vue
@apps/web/src/composables/useHistoryRestore.ts
@apps/web/src/composables/useHistoryAutoSave.ts
@apps/web/src/stores/useHistoryStore.ts
@apps/web/src/App.vue
@playwright.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create history restore E2E test</name>
  <files>apps/web/e2e/history-restore.spec.ts</files>
  <action>
Create a new Playwright E2E spec file `apps/web/e2e/history-restore.spec.ts` that tests the full history restore flow.

Follow the EXACT same patterns as url-sharing.spec.ts:
- Import GENE_SEARCH_RESPONSE, GENE_DETAILS_RESPONSE, GENE_VARIANTS_RESPONSE from `./fixtures/gnomad-responses`
- Reuse the same interceptGnomadApi() and dismissDisclaimer() helper patterns (copy from url-sharing.spec.ts — these are test-local helpers, not shared modules)
- Also intercept ClinGen CSV and search.clinicalgenome.org routes (same pattern as url-sharing.spec.ts)
- Reuse the navigateToStep4() helper pattern (same wizard navigation as url-sharing.spec.ts)

Test flow — single test case "completing a calculation and restoring from history shows Step 4 results":

1. **Navigate to Step 4** via navigateToStep4() helper — this completes the CFTR wizard flow with mocked API responses. The useHistoryAutoSave composable automatically saves to the Pinia history store (persisted to localStorage with key 'carrier-freq-history') when entering Step 4.

2. **Verify history entry was created** — After reaching Step 4, the auto-save should have created a history entry. The history button in AppBar has `data-testid="footer-history-btn"`.

3. **Open the HistoryDrawer** — Click the history button (`data-testid="footer-history-btn"`) in the AppBar. Wait for the HistoryDrawer (`data-testid="history-drawer"`) to become visible. The drawer is a v-navigation-drawer from the right.

4. **Verify history panel shows entries** — The HistoryPanel (`data-testid="history-panel"`) should be visible inside the drawer. It should contain at least one entry with `data-testid="history-entry"`. The entry should contain text "CFTR" (the gene symbol is displayed in v-list-item-title).

5. **Click the history entry to restore** — Click the first `data-testid="history-entry"` element. This triggers: HistoryPanel emits 'restore' with entry.id -> HistoryDrawer emits 'restore' -> App.vue calls handleHistoryRestore -> useHistoryRestore.restoreFromHistory() sets wizardState.currentStep = 4 and restores all state.

6. **Verify state restored to Step 4** — After clicking, the drawer should close (HistoryDrawer sets modelValue = false before emitting restore). The wizard should show Step 4: `data-testid="step-results"` visible, `data-testid="results-summary-card"` visible and containing "CFTR".

Important implementation notes:
- The history auto-save has a 30-second deduplication window — since we're restoring the same CFTR entry that was just saved, the restoreFromHistory flow does NOT re-save (it calls saveCurrentCalculation() but the duplicate check passes). This is fine for the test.
- The restoreFromHistory() function calls geneSearch.selectGene() and setGeneSymbol() which trigger gnomAD API fetches. The interceptGnomadApi route handler is still active on the same page, so the mocked responses will be served.
- Use generous timeouts (10-15 seconds) for result restoration since it involves reactive state propagation through multiple composables.
- Do NOT navigate away from Step 4 between saving and restoring. The test should: reach Step 4 -> open drawer -> click entry -> verify Step 4 is still showing with correct data. The restore flow works even when already on Step 4 because it clears gene to null first, then restores everything.

Alternative stronger test approach (preferred if feasible): After reaching Step 4, navigate back to Step 1 (click on step 1 in the stepper or reload without URL params), THEN open history drawer and click entry, verifying it jumps from Step 1 to Step 4. This more convincingly proves restore works. To navigate back: use `page.goto('/')` followed by dismissDisclaimer again, which starts fresh at Step 1. The history persists in localStorage across page loads.

Use the stronger approach:
1. navigateToStep4() — completes wizard, auto-saves to history
2. page.goto('/') — fresh page load (history persists in localStorage)
3. interceptGnomadApi(page) again — re-register route handlers for new page context
4. dismissDisclaimer(page) — disclaimer reappears on fresh load
5. Verify we're on Step 1 (step-gene visible)
6. Click footer-history-btn to open drawer
7. Click history-entry to restore
8. Verify step-results visible with CFTR data

Wrap in test.describe('History restore', () => { ... }).
  </action>
  <verify>
Run `npx playwright test apps/web/e2e/history-restore.spec.ts --headed` (if dev server running) or verify the test file structure matches existing E2E patterns. The test should pass when run against the dev server with: `cd apps/web && bun run dev` in one terminal, then `npx playwright test apps/web/e2e/history-restore.spec.ts` in another.

Note: E2E tests require a running dev server or built preview. If the dev server is not running, verify the file compiles by checking `npx tsc --noEmit` on the playwright config scope, or verify by structural review that the test follows the exact same patterns as the verified url-sharing.spec.ts.
  </verify>
  <done>
apps/web/e2e/history-restore.spec.ts exists with a Playwright test that: (1) completes CFTR wizard to Step 4, (2) navigates to fresh page, (3) opens HistoryDrawer, (4) clicks saved entry, (5) verifies state restores to Step 4 with CFTR results. Test follows same API interception and helper patterns as url-sharing.spec.ts.
  </done>
</task>

</tasks>

<verification>
- `apps/web/e2e/history-restore.spec.ts` exists and is >= 80 lines
- File imports from `./fixtures/gnomad-responses` (same as other E2E specs)
- File uses `data-testid="footer-history-btn"`, `data-testid="history-drawer"`, `data-testid="history-entry"`, `data-testid="step-results"`, `data-testid="results-summary-card"`
- File intercepts gnomAD API with same route pattern as url-sharing.spec.ts
- Test flow covers: navigate to Step 4 -> fresh page load -> open drawer -> click entry -> verify Step 4 CFTR results
</verification>

<success_criteria>
- History restore E2E test file exists and follows established patterns
- Test exercises the full user flow: wizard completion -> history drawer -> entry click -> state restoration
- TEST-10 requirement (history restore) is now covered at E2E level
</success_criteria>

<output>
After completion, create `.planning/phases/29-test-suite-completion/29-06-SUMMARY.md`
</output>
