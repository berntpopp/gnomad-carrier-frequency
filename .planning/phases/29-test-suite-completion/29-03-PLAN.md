---
phase: 29-test-suite-completion
plan: 03
type: execute
wave: 2
depends_on: ["29-01"]
files_modified:
  - apps/web/src/components/__tests__/FilterPanel.test.ts
  - apps/web/src/components/__tests__/AppBar.test.ts
  - apps/web/src/components/__tests__/VersionSelector.test.ts
  - apps/web/src/components/__tests__/SettingsDialog.test.ts
  - apps/web/src/components/__tests__/TemplateEditor.test.ts
autonomous: true

must_haves:
  truths:
    - "FilterPanel renders filter switches and emits config changes on toggle"
    - "AppBar renders app title, gene context chip when gene is selected, and settings/history buttons"
    - "VersionSelector renders gnomAD version options"
    - "SettingsDialog renders with tabs for general, filters, and templates settings"
    - "TemplateEditor renders template editing interface"
  artifacts:
    - path: "apps/web/src/components/__tests__/FilterPanel.test.ts"
      provides: "Filter panel interaction tests"
      contains: "mountWithPlugins"
    - path: "apps/web/src/components/__tests__/AppBar.test.ts"
      provides: "App bar rendering tests"
      contains: "gene-context-chip"
    - path: "apps/web/src/components/__tests__/SettingsDialog.test.ts"
      provides: "Settings dialog tab rendering tests"
      contains: "settings-dialog"
  key_links:
    - from: "apps/web/src/components/__tests__/FilterPanel.test.ts"
      to: "apps/web/src/components/FilterPanel.vue"
      via: "component import"
      pattern: "import.*FilterPanel"
    - from: "apps/web/src/components/__tests__/*.test.ts"
      to: "apps/web/src/test/helpers.ts"
      via: "mountWithPlugins import"
      pattern: "import.*mountWithPlugins"
---

<objective>
Write Vue Test Utils component tests for settings, filter, and chrome components.

Purpose: Verify the non-wizard interactive components (filter toggles, settings dialog tabs, app bar navigation) render and respond to user interactions correctly. These components handle configuration that affects the entire calculation pipeline.

Output: 5 passing component test files covering FilterPanel, AppBar, VersionSelector, SettingsDialog, and TemplateEditor.
</objective>

<execution_context>
@C:\Users\bernt\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\bernt\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-test-suite-completion/29-CONTEXT.md
@.planning/phases/29-test-suite-completion/29-RESEARCH.md
@.planning/phases/29-test-suite-completion/29-01-SUMMARY.md

Key source files to reference:
@apps/web/src/test/helpers.ts (mountWithPlugins)
@apps/web/src/components/FilterPanel.vue
@apps/web/src/components/AppBar.vue
@apps/web/src/components/VersionSelector.vue
@apps/web/src/components/SettingsDialog.vue
@apps/web/src/components/TemplateEditor.vue
@apps/web/src/stores/useFilterStore.ts
@apps/web/src/stores/useAppStore.ts
@apps/web/src/stores/useTemplateStore.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write FilterPanel and AppBar component tests</name>
  <files>
    apps/web/src/components/__tests__/FilterPanel.test.ts
    apps/web/src/components/__tests__/AppBar.test.ts
  </files>
  <action>
    Read each component source file FIRST to understand props, emits, data-testid attributes, store reads, and child component usage.

    **FilterPanel.test.ts:**
    - Read `FilterPanel.vue` to understand its prop interface (modelValue: FilterConfig, calcConfig: CalcConfig, variantCount: number, etc.) and emitted events
    - Import FilterConfig and CalcConfig types from `@gnomad-cf/core/types`
    - Create default config objects matching the types
    - Test cases:
      - Renders without errors when given valid props
      - The expansion panel can be opened (trigger click on panel title)
      - LoF HC switch is present and reflects the `lofHcEnabled` prop
      - Emits `update:modelValue` when a filter switch is toggled (e.g., toggling missense)
      - Emits `update:calcConfig` when a calc setting changes (e.g., HWE formula toggle)
      - If gene config indicator exists in FilterPanel, verify it renders when gene config data is provided via props
    - Use `mountWithPlugins` with appropriate props
    - FilterPanel likely uses `v-expansion-panel` which may require interaction to reveal inner content

    **AppBar.test.ts:**
    - Read `AppBar.vue` to understand its structure, gene context chip logic, and button clicks
    - AppBar reads from stores or receives props — determine which by reading source
    - Test cases:
      - Renders the app bar container
      - Shows history button (`[data-testid="footer-history-btn"]`)
      - Shows settings button (`[data-testid="footer-settings-btn"]`)
      - Shows gene context chip (`[data-testid="gene-context-chip"]`) when gene is selected in wizard state
    - Reset `useWizard()` in beforeEach if AppBar reads wizard state
    - For the gene context chip test, set wizard state to have a selected gene before mounting

    IMPORTANT: Read the .vue files to verify exact prop names, event names, and data-testid values. Adapt tests to match source code, not this plan's assumptions.
  </action>
  <verify>
    - `bun run test --filter gnomad-cf-web` passes FilterPanel and AppBar tests
    - No "Cannot resolve" errors for `@gnomad-cf/core/types` imports
  </verify>
  <done>
    FilterPanel test verifies filter toggles emit config changes. AppBar test verifies chrome buttons and gene context chip render.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write VersionSelector, SettingsDialog, and TemplateEditor component tests</name>
  <files>
    apps/web/src/components/__tests__/VersionSelector.test.ts
    apps/web/src/components/__tests__/SettingsDialog.test.ts
    apps/web/src/components/__tests__/TemplateEditor.test.ts
  </files>
  <action>
    Read each component source file FIRST.

    **VersionSelector.test.ts:**
    - Read `VersionSelector.vue` to understand its store reads and rendering
    - Test cases:
      - Renders without errors
      - Displays current gnomAD version from store state
      - Contains selectable version options
    - Use `mountWithPlugins` with `storeInitialState` to seed app store with a gnomAD version

    **SettingsDialog.test.ts:**
    - Read `SettingsDialog.vue` to understand its tab structure and dialog behavior
    - This is a `v-dialog` component — Vuetify teleports dialog content to `document.body`
    - Use `attachTo: document.body` in mount options so teleported content is findable
    - Control dialog visibility via `modelValue` prop (if it accepts one) or by setting the dialog's internal state
    - Test cases:
      - Renders the dialog container (`[data-testid="settings-dialog"]`)
      - Contains general settings tab (`[data-testid="settings-tab-general"]`)
      - Contains filters settings tab (`[data-testid="settings-tab-filters"]`)
      - Contains templates settings tab (`[data-testid="settings-tab-templates"]`)
    - Stub child components within tabs if they're complex (e.g., TemplateEditor, FilterPanel) to keep this test focused on tab rendering
    - Clean up after test: `wrapper.unmount()` to remove teleported content from document.body

    **TemplateEditor.test.ts:**
    - Read `TemplateEditor.vue` to understand its editing interface
    - Test cases:
      - Renders without errors when template store has default templates
      - Displays template sections based on store state
    - Seed template store with `storeInitialState` providing language and enabled sections

    IMPORTANT: For teleported components (v-dialog), use `document.body.querySelector()` or `attachTo: document.body` mount option. Do NOT expect `wrapper.find()` to locate teleported content.
  </action>
  <verify>
    - `bun run test --filter gnomad-cf-web` passes all component tests (stores + wizard + settings/chrome)
    - SettingsDialog test handles Vuetify teleportation correctly
    - No leaked DOM nodes (wrapper.unmount called in afterEach for attachTo tests)
  </verify>
  <done>
    VersionSelector, SettingsDialog, and TemplateEditor component tests pass. Settings dialog tabs are verified. All interactive components from the CONTEXT.md test scope are covered.
  </done>
</task>

</tasks>

<verification>
- `bun run test --filter gnomad-cf-web` passes all tests (stores from Plan 01 + wizard from Plan 02 + settings/chrome from this plan)
- Total web test count is 15+ (5 store files + 6 wizard files + 5 settings/chrome files = 16 test files)
- No order-dependent test failures when running full suite
</verification>

<success_criteria>
- 5 component test files created in `apps/web/src/components/__tests__/`
- FilterPanel tests verify filter toggle emissions
- AppBar tests verify chrome rendering and gene context chip
- SettingsDialog tests handle Vuetify teleportation and verify tab rendering
- VersionSelector tests verify version display
- TemplateEditor tests verify template editing interface renders
</success_criteria>

<output>
After completion, create `.planning/phases/29-test-suite-completion/29-03-SUMMARY.md`
</output>
