---
phase: 29-test-suite-completion
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/vitest.config.ts
  - packages/cli/vitest.config.ts
  - apps/web/vitest.config.ts
  - .github/workflows/tests.yml
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Vitest prints a threshold violation warning when coverage falls below target values"
    - "CI does not fail when coverage is below threshold (warn-only behavior)"
  artifacts:
    - path: "packages/core/vitest.config.ts"
      provides: "Core coverage thresholds at 90"
      contains: "lines: 90"
    - path: "packages/cli/vitest.config.ts"
      provides: "CLI coverage thresholds at 80"
      contains: "lines: 80"
    - path: "apps/web/vitest.config.ts"
      provides: "Web coverage thresholds at 40"
      contains: "lines: 40"
    - path: ".github/workflows/tests.yml"
      provides: "continue-on-error on test steps"
      contains: "continue-on-error: true"
  key_links:
    - from: "packages/core/vitest.config.ts"
      to: ".github/workflows/tests.yml"
      via: "bun run --filter @gnomad-cf/core test -- --coverage"
      pattern: "continue-on-error.*true"
    - from: "packages/cli/vitest.config.ts"
      to: ".github/workflows/tests.yml"
      via: "bun run --filter @gnomad-cf/cli test -- --coverage"
      pattern: "continue-on-error.*true"
    - from: "apps/web/vitest.config.ts"
      to: ".github/workflows/tests.yml"
      via: "bun run --filter gnomad-cf-web test -- --coverage"
      pattern: "continue-on-error.*true"
---

<objective>
Set real coverage thresholds in all three vitest configs and add continue-on-error to CI test steps, closing Gap 2 from VERIFICATION.md.

Purpose: ROADMAP criterion 4 requires "coverage thresholds are enforced and a build with coverage below threshold warns in CI." Currently all vitest thresholds are 0, which means vitest never prints a threshold violation. Setting real values (core: 90, CLI: 80, web: 40) makes vitest emit warnings when coverage drops. Adding continue-on-error: true to CI test steps ensures the build doesn't fail (warn-only behavior per CONTEXT.md).

Output: Three updated vitest configs with real thresholds + updated CI workflow with continue-on-error on test steps.
</objective>

<execution_context>
@C:\Users\bernt\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\bernt\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-test-suite-completion/29-05-SUMMARY.md
@packages/core/vitest.config.ts
@packages/cli/vitest.config.ts
@apps/web/vitest.config.ts
@.github/workflows/tests.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Set real coverage thresholds in vitest configs</name>
  <files>packages/core/vitest.config.ts, packages/cli/vitest.config.ts, apps/web/vitest.config.ts</files>
  <action>
Update the `thresholds` block in all three vitest config files to use the actual target values from CONTEXT.md. Only set `lines` threshold (the primary metric); leave functions, branches, and statements at 0 to avoid false positives on metrics that were never targeted.

**packages/core/vitest.config.ts** — Change thresholds to:
```typescript
thresholds: {
  lines: 90,
  functions: 0,
  branches: 0,
  statements: 0,
},
```
Update the comment from `// Target: 90%+ (warn-only — thresholds at 0 so build doesn't fail)` to `// Warn-only: vitest prints violation; CI uses continue-on-error`

**packages/cli/vitest.config.ts** — Change thresholds to:
```typescript
thresholds: {
  lines: 80,
  functions: 0,
  branches: 0,
  statements: 0,
},
```
Update the comment from `// Target: 80%+ (warn-only — thresholds at 0 so build doesn't fail)` to `// Warn-only: vitest prints violation; CI uses continue-on-error`

**apps/web/vitest.config.ts** — Change thresholds to:
```typescript
thresholds: {
  lines: 40,
  functions: 0,
  branches: 0,
  statements: 0,
},
```
Update the comment from `// Thresholds set to 0 (warn-only). Target: 40%+ web coverage` to `// Warn-only: vitest prints violation; CI uses continue-on-error`

Do NOT change any other config values — only the thresholds numbers and the associated comments.
  </action>
  <verify>
Run `bun run test -- --coverage` from the repo root (or per-package) and verify that vitest now prints coverage percentages and does NOT show "ERROR: Coverage..." threshold violations (since current coverage should exceed targets). If any package is below threshold, vitest will print a clear warning line — this is the desired behavior.

Specifically:
- `bun run --filter @gnomad-cf/core test -- --coverage` should show lines coverage and pass (current core coverage is well above 90%)
- `bun run --filter @gnomad-cf/cli test -- --coverage` should show lines coverage
- `bun run --filter gnomad-cf-web test -- --coverage` should show lines coverage
  </verify>
  <done>
All three vitest configs have real line thresholds (core: 90, CLI: 80, web: 40). Vitest will now print threshold violation warnings when coverage drops below target. Tests still pass with current coverage levels.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add continue-on-error to CI test steps for warn-only behavior</name>
  <files>.github/workflows/tests.yml</files>
  <action>
Add `continue-on-error: true` to the three test-with-coverage steps in `.github/workflows/tests.yml`. This ensures that if vitest exits non-zero due to a threshold violation, the CI workflow continues and reports warnings without failing the build.

Update these three steps by adding `continue-on-error: true`:

```yaml
- name: Run core tests with coverage
  continue-on-error: true
  run: bun run --filter @gnomad-cf/core test -- --coverage

- name: Run CLI tests with coverage
  continue-on-error: true
  run: bun run --filter @gnomad-cf/cli test -- --coverage

- name: Run web component tests with coverage
  continue-on-error: true
  run: bun run --filter gnomad-cf-web test -- --coverage
```

Also update the "Coverage summary" step to clarify that thresholds are now enforced by vitest (not just advisory text):

```yaml
- name: Coverage summary
  if: always()
  run: |
    echo "## Coverage Thresholds (warn-only)"
    echo "Vitest enforces these thresholds — violations appear as errors in test output above."
    echo "- Core: 90%+ lines"
    echo "- CLI: 80%+ lines"
    echo "- Web: 40%+ lines"
    echo ""
    echo "Steps use continue-on-error so threshold violations warn but do not fail the build."
```

Do NOT change any other workflow configuration (triggers, E2E steps, checkout, setup-bun, etc.).

Important: Do NOT add continue-on-error to the E2E test step — E2E test failures should still fail CI. Only the coverage threshold steps get continue-on-error.
  </action>
  <verify>
Read `.github/workflows/tests.yml` and verify:
1. The three test steps (core, CLI, web) each have `continue-on-error: true`
2. The E2E step does NOT have continue-on-error
3. The coverage summary text reflects that vitest enforces thresholds
4. No other workflow structure was changed
  </verify>
  <done>
CI workflow has continue-on-error: true on all three test-with-coverage steps. Vitest threshold violations will print warnings in CI logs but will not fail the build. E2E failures still fail CI.
  </done>
</task>

</tasks>

<verification>
- `grep -n "lines: 90" packages/core/vitest.config.ts` returns a match
- `grep -n "lines: 80" packages/cli/vitest.config.ts` returns a match
- `grep -n "lines: 40" apps/web/vitest.config.ts` returns a match
- `grep -n "continue-on-error" .github/workflows/tests.yml` returns 3 matches (one per test step)
- `bun run --filter @gnomad-cf/core test -- --coverage` passes without threshold error
- `bun run --filter @gnomad-cf/cli test -- --coverage` passes without threshold error
- `bun run --filter gnomad-cf-web test -- --coverage` passes without threshold error
</verification>

<success_criteria>
- All three vitest configs have real line thresholds matching CONTEXT.md targets
- CI test steps use continue-on-error: true for warn-only behavior
- Vitest will now print threshold violation warnings when coverage drops below targets
- CI build does not fail due to coverage thresholds (warn-only)
- TEST-12 requirement (coverage thresholds enforced) is now satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/29-test-suite-completion/29-07-SUMMARY.md`
</output>
