---
phase: 29-test-suite-completion
plan: 02
type: execute
wave: 2
depends_on: ["29-01"]
files_modified:
  - apps/web/src/components/wizard/__tests__/StepGene.test.ts
  - apps/web/src/components/wizard/__tests__/StepStatus.test.ts
  - apps/web/src/components/wizard/__tests__/StepFrequency.test.ts
  - apps/web/src/components/wizard/__tests__/StepResults.test.ts
  - apps/web/src/components/wizard/__tests__/WizardStepper.test.ts
  - apps/web/src/components/wizard/__tests__/TextOutput.test.ts
autonomous: true

must_haves:
  truths:
    - "Wizard step components render without errors when mounted with Vuetify + Pinia"
    - "StepGene renders gene search input and next button"
    - "StepStatus renders carrier status radio options and emits selection"
    - "StepFrequency renders frequency data and navigation controls"
    - "StepResults renders summary card, population table, and clinical text"
    - "WizardStepper renders all 4 step headers"
  artifacts:
    - path: "apps/web/src/components/wizard/__tests__/StepGene.test.ts"
      provides: "Gene search step component tests"
      contains: "mountWithPlugins"
    - path: "apps/web/src/components/wizard/__tests__/StepStatus.test.ts"
      provides: "Status selection step component tests"
      contains: "step-status"
    - path: "apps/web/src/components/wizard/__tests__/StepResults.test.ts"
      provides: "Results display component tests"
      contains: "results-summary-card"
  key_links:
    - from: "apps/web/src/components/wizard/__tests__/*.test.ts"
      to: "apps/web/src/test/helpers.ts"
      via: "mountWithPlugins import"
      pattern: "import.*mountWithPlugins.*from.*test/helpers"
    - from: "apps/web/src/components/wizard/__tests__/*.test.ts"
      to: "apps/web/src/composables/useWizard.ts"
      via: "resetWizard in beforeEach"
      pattern: "resetWizard"
---

<objective>
Write Vue Test Utils component tests for all wizard step components.

Purpose: Verify the core 4-step wizard flow renders correctly and handles key interactions after the monorepo restructure. These are the most critical components in the app — if the wizard breaks, the entire tool is unusable.

Output: 6 passing component test files covering StepGene, StepStatus, StepFrequency, StepResults, WizardStepper, and TextOutput.
</objective>

<execution_context>
@C:\Users\bernt\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\bernt\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-test-suite-completion/29-CONTEXT.md
@.planning/phases/29-test-suite-completion/29-RESEARCH.md
@.planning/phases/29-test-suite-completion/29-01-SUMMARY.md

Key source files to reference:
@apps/web/src/test/helpers.ts (mountWithPlugins)
@apps/web/src/test/setup.ts (vuetify instance)
@apps/web/src/components/wizard/StepGene.vue
@apps/web/src/components/wizard/StepStatus.vue
@apps/web/src/components/wizard/StepFrequency.vue
@apps/web/src/components/wizard/StepResults.vue
@apps/web/src/components/wizard/WizardStepper.vue
@apps/web/src/components/wizard/TextOutput.vue
@apps/web/src/composables/useWizard.ts
@apps/web/src/composables/useGeneSearch.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write wizard step component tests (StepGene, StepStatus, StepFrequency)</name>
  <files>
    apps/web/src/components/wizard/__tests__/StepGene.test.ts
    apps/web/src/components/wizard/__tests__/StepStatus.test.ts
    apps/web/src/components/wizard/__tests__/StepFrequency.test.ts
  </files>
  <action>
    Read each component source file FIRST to understand props, emits, data-testid attributes, and composable usage. Then write tests.

    CRITICAL for all wizard tests: Call `resetWizard()` from `useWizard()` in `beforeEach` to clear module-level singleton state. Also call `clearSelection()` from `useGeneSearch()` if the component uses gene search.

    **StepGene.test.ts:**
    - Read `StepGene.vue` to understand props/emits. This component uses `GeneSearch` child component and `useWizard`.
    - Stub `GeneSearch` child component (it makes API calls via villus) using `global.stubs: { GeneSearch: true }` in mount options
    - Test cases:
      - Renders the step container (`[data-testid="step-gene"]` exists)
      - Renders next button (`[data-testid="step-gene-next-btn"]` exists)
      - Next button is disabled when no gene is selected (check disabled attribute or class)
    - Use `mountWithPlugins` with appropriate `storeInitialState` for the app/wizard stores

    **StepStatus.test.ts:**
    - Read `StepStatus.vue` to understand props/emits and radio option structure.
    - Test cases:
      - Renders step container (`[data-testid="step-status"]` exists)
      - Renders the heterozygous status option (`[data-testid="status-option-heterozygous"]` exists)
      - Renders next button (`[data-testid="step-status-next-btn"]` exists)
      - Next button click emits the expected event (check `wrapper.emitted()`)
    - Mount with `mountWithPlugins`, pass props based on component's prop interface

    **StepFrequency.test.ts:**
    - Read `StepFrequency.vue` to understand props/emits and tab structure.
    - This component may use `useGeneVariants` or `useCarrierFrequency` composables — understand what state it reads
    - Test cases:
      - Renders step container (`[data-testid="step-frequency"]` exists)
      - Renders gnomAD tab (`[data-testid="freq-tab-gnomad"]` exists)
      - Renders next button (`[data-testid="step-frequency-next-btn"]` exists)
    - If the component reads from stores, seed initial state via `storeInitialState` in mountWithPlugins
    - If it uses composables with module-level state, mock or reset them in beforeEach

    IMPORTANT: Read each .vue file before writing tests. The data-testid attributes listed here are from a grep — verify they're still correct and understand the component structure. Do NOT write tests based solely on this plan's assumptions; verify from source.
  </action>
  <verify>
    - `bun run test --filter gnomad-cf-web` passes all 3 wizard step test files
    - No "Cannot find module" errors (aliases resolve correctly)
    - No Vuetify provide/inject warnings
  </verify>
  <done>
    StepGene, StepStatus, and StepFrequency component tests pass, verifying rendering and key interactions.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write StepResults, WizardStepper, and TextOutput component tests</name>
  <files>
    apps/web/src/components/wizard/__tests__/StepResults.test.ts
    apps/web/src/components/wizard/__tests__/WizardStepper.test.ts
    apps/web/src/components/wizard/__tests__/TextOutput.test.ts
  </files>
  <action>
    Read each component source file FIRST. Apply same patterns as Task 1 (resetWizard in beforeEach, mountWithPlugins, real Vuetify).

    **StepResults.test.ts:**
    - Read `StepResults.vue` to understand its props, store reads, and composable usage. This is the most complex wizard step — it renders frequency results, population table, filter panel, variant table, and text output.
    - Stub heavy child components that are tested separately: `FilterPanel`, `VariantTable`, `TextOutput`, `FrequencyResults` — use `global.stubs` to avoid cascading complexity
    - Seed pinia stores with realistic data (filter store, calc store) via `storeInitialState`
    - Test cases:
      - Renders step container (`[data-testid="step-results"]` exists)
      - Renders summary card (`[data-testid="results-summary-card"]` exists) when frequency data is seeded
      - Renders population table container (`[data-testid="population-table"]` exists)
    - If StepResults reads frequency data from a composable, seed the underlying store or mock the composable return

    **WizardStepper.test.ts:**
    - Read `WizardStepper.vue` to understand step rendering and navigation logic.
    - Stub individual step components (StepGene, StepStatus, StepFrequency, StepResults) — WizardStepper orchestrates, individual steps are tested separately
    - Test cases:
      - Renders stepper container (`[data-testid="wizard-stepper"]` exists)
      - Renders all 4 step headers (data-testid wizard-step-1 through wizard-step-4 exist)
      - Renders wizard content area (`[data-testid="wizard-content"]` exists)

    **TextOutput.test.ts:**
    - Read `TextOutput.vue` to understand its props and template rendering.
    - Test cases:
      - Renders text output container (`[data-testid="text-output"]` exists)
      - Renders section chips area (`[data-testid="text-section-chips"]` exists)
      - Renders text content area (`[data-testid="text-content"]` exists)
    - Seed template store with language/sections so the component has data to render

    IMPORTANT: Read each .vue file before writing tests. If a component's interface differs from what's described here, follow the source code. The source is the truth, not this plan.
  </action>
  <verify>
    - `bun run test --filter gnomad-cf-web` passes all 6 wizard component test files (3 from Task 1 + 3 from Task 2)
    - No test takes longer than 5 seconds individually
  </verify>
  <done>
    All 6 wizard component test files pass. StepResults, WizardStepper, and TextOutput render correctly with stubbed child components and seeded store state.
  </done>
</task>

</tasks>

<verification>
- `bun run test --filter gnomad-cf-web` passes all wizard tests + store tests from Plan 01
- Each test file has at least 2 test cases
- No Vuetify warnings about missing provide/inject
- Wizard singleton state is properly reset between tests (no order-dependent failures)
</verification>

<success_criteria>
- 6 wizard component test files created in `apps/web/src/components/wizard/__tests__/`
- All tests pass when run together and individually
- Components mount with real Vuetify (not shallowMount)
- Heavy child components are stubbed to keep tests focused
- useWizard and useGeneSearch singleton state reset in beforeEach
</success_criteria>

<output>
After completion, create `.planning/phases/29-test-suite-completion/29-02-SUMMARY.md`
</output>
