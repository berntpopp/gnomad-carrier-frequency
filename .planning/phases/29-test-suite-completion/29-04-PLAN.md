---
phase: 29-test-suite-completion
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - playwright.config.ts
  - apps/web/e2e/fixtures/gnomad-responses.ts
  - apps/web/e2e/cftr-wizard.spec.ts
  - apps/web/e2e/url-sharing.spec.ts
autonomous: true

must_haves:
  truths:
    - "Playwright runs E2E tests against the local dev server with mocked gnomAD API"
    - "CFTR wizard happy path completes all 4 steps and displays clinical text containing CFTR"
    - "URL state roundtrip preserves wizard state across page navigation"
  artifacts:
    - path: "playwright.config.ts"
      provides: "Playwright configuration targeting apps/web dev server"
      contains: "testDir.*apps/web/e2e"
    - path: "apps/web/e2e/fixtures/gnomad-responses.ts"
      provides: "TypeScript fixtures matching gnomAD API response shapes"
      exports: ["GENE_SEARCH_RESPONSE", "GENE_DETAILS_RESPONSE", "GENE_VARIANTS_RESPONSE"]
    - path: "apps/web/e2e/cftr-wizard.spec.ts"
      provides: "CFTR wizard happy path E2E test"
      contains: "operationName"
    - path: "apps/web/e2e/url-sharing.spec.ts"
      provides: "URL state roundtrip E2E test"
      contains: "gene=CFTR"
  key_links:
    - from: "apps/web/e2e/cftr-wizard.spec.ts"
      to: "apps/web/e2e/fixtures/gnomad-responses.ts"
      via: "fixture imports"
      pattern: "import.*from.*fixtures"
    - from: "playwright.config.ts"
      to: "apps/web"
      via: "webServer command"
      pattern: "bun run dev"
    - from: "apps/web/e2e/cftr-wizard.spec.ts"
      to: "gnomad.broadinstitute.org"
      via: "page.route API interception"
      pattern: "page\\.route.*gnomad"
---

<objective>
Create Playwright E2E tests for the CFTR wizard happy path and URL state roundtrip.

Purpose: Verify the full application works end-to-end in a real browser — gene search, wizard navigation, carrier frequency calculation, and clinical text generation. This catches integration issues that component tests miss (routing, real DOM rendering, cross-component state flow).

Output: Playwright config, gnomAD API fixtures, and 2 passing E2E test files.
</objective>

<execution_context>
@C:\Users\bernt\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\bernt\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-test-suite-completion/29-CONTEXT.md
@.planning/phases/29-test-suite-completion/29-RESEARCH.md

Key source files to reference:
@apps/web/vite.config.ts (dev server config, port 5173)
@apps/web/src/components/wizard/StepGene.vue (gene search interaction)
@apps/web/src/components/wizard/StepStatus.vue (status selection)
@apps/web/src/components/wizard/StepFrequency.vue (frequency display)
@apps/web/src/components/wizard/StepResults.vue (results with clinical text)
@apps/web/src/components/GeneSearch.vue (autocomplete interaction)
@packages/core/src/queries/ (GraphQL query operation names)
@packages/core/src/types/ (response type shapes for fixtures)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Playwright config and gnomAD API fixtures</name>
  <files>
    playwright.config.ts
    apps/web/e2e/fixtures/gnomad-responses.ts
  </files>
  <action>
    1. Recreate `playwright.config.ts` at repo root (was deleted from git):
       ```typescript
       import { defineConfig, devices } from '@playwright/test'

       export default defineConfig({
         testDir: './apps/web/e2e',
         fullyParallel: false,           // sequential — mocked API tests share route handlers
         forbidOnly: !!process.env.CI,
         retries: process.env.CI ? 1 : 0,
         timeout: 30_000,
         use: {
           baseURL: 'http://localhost:5173',
           trace: 'on-first-retry',
         },
         projects: [
           { name: 'chromium', use: { ...devices['Desktop Chrome'] } },
         ],
         webServer: {
           command: 'bun run dev',
           url: 'http://localhost:5173',
           reuseExistingServer: !process.env.CI,
           timeout: 60_000,
         },
       })
       ```

    2. Create `apps/web/e2e/fixtures/gnomad-responses.ts` with TypeScript fixture data:
       - First, read the GraphQL query files in `packages/core/src/queries/` to find exact `operationName` values used in queries. Also read `packages/core/src/types/` to understand response shapes.
       - Create fixtures for 3 operations:
         - `GENE_SEARCH_RESPONSE`: Matches GeneSearch query response shape. Contains `gene_search` array with one entry: `{ ensembl_id: 'ENSG00000001626', symbol: 'CFTR' }` (plus any other required fields from the type).
         - `GENE_DETAILS_RESPONSE`: Matches GeneDetails query response. Contains `gene` object with `gene_id`, `symbol: 'CFTR'`, and `gnomad_constraint` (null or realistic mock).
         - `GENE_VARIANTS_RESPONSE`: Matches GeneVariants query response. Contains `gene.variants` array with 2-3 CFTR-like variants including:
           - At least one LoF HC variant with `transcript_consequence.lof: 'HC'` and `consequence_terms: ['stop_gained']`
           - At least one ClinVar pathogenic variant in `gene.clinvar_variants` with matching `variant_id`, `clinical_significance: 'Pathogenic'`, `gold_stars: 3`
           - Realistic `exome` frequencies: `ac`, `an`, `ac_hom`, and `populations` array with `nfe` and `afr` entries
           - The fixture must produce a non-zero carrier frequency when the standard filters are applied (LoF HC + ClinVar pathogenic)
       - Export all 3 as named exports

    3. Ensure `@playwright/test` is already available (it's in root devDependencies at `^1.58.2`). Verify Playwright browsers are installed locally:
       ```
       npx playwright install chromium
       ```
       This only needs to run once locally. CI will have its own install step (Plan 05).

    IMPORTANT: The fixture data shapes MUST match the actual GraphQL response types. Read the type definitions in `packages/core/src/types/` before writing fixtures. Incorrect shapes will cause silent failures where the app receives data but can't process it.
  </action>
  <verify>
    - `playwright.config.ts` exists at repo root with testDir `./apps/web/e2e`
    - `apps/web/e2e/fixtures/gnomad-responses.ts` exports all 3 fixture objects
    - Playwright config references correct webServer command and URL
    - TypeScript compilation of fixture file succeeds (types match)
  </verify>
  <done>
    Playwright config recreated with correct settings. Fixture file provides type-safe CFTR gnomAD API responses for all 3 GraphQL operations.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write CFTR wizard happy path and URL roundtrip E2E tests</name>
  <files>
    apps/web/e2e/cftr-wizard.spec.ts
    apps/web/e2e/url-sharing.spec.ts
  </files>
  <action>
    FIRST: Read the wizard step Vue components to understand the exact user interaction flow: what gets clicked, what appears, what selectors to use. The data-testid attributes are already in place.

    **cftr-wizard.spec.ts — CFTR Happy Path:**

    1. Set up API interception in `test.beforeEach`:
       - `await page.route('**/gnomad.broadinstitute.org/api**', async (route) => { ... })`
       - Parse `route.request().postDataJSON()` to get `operationName`
       - Route to correct fixture: GeneSearch, GeneDetails (or GeneInfo — verify actual name), GeneVariants
       - Use `route.fallback()` for unmatched operations (NOT `route.continue()`)

    2. Test: "CFTR wizard completes full 4-step flow with clinical text"
       - `await page.goto('/')`
       - **Step 1 (Gene):** Read `StepGene.vue` and `GeneSearch.vue` to understand the gene search interaction:
         - Type 'CFTR' into the gene search input (`[data-testid="gene-search-input"] input`)
         - Wait for and click the autocomplete result showing 'CFTR'
         - Click next button (`[data-testid="step-gene-next-btn"]`)
       - **Step 2 (Status):** Read `StepStatus.vue`:
         - Default carrier status may already be selected — verify by reading source
         - Click next button (`[data-testid="step-status-next-btn"]`)
       - **Step 3 (Frequency):** Read `StepFrequency.vue`:
         - Wait for variants to load (the route interception will fulfill the request)
         - Wait for frequency display to appear
         - Click next button (`[data-testid="step-frequency-next-btn"]`)
       - **Step 4 (Results):** Read `StepResults.vue` and `TextOutput.vue`:
         - Assert results summary card is visible: `await expect(page.locator('[data-testid="results-summary-card"]')).toBeVisible()`
         - Assert text output section is visible: `await expect(page.locator('[data-testid="text-output"]')).toBeVisible()`
         - Assert clinical text contains 'CFTR': `await expect(page.locator('[data-testid="text-content"]')).toContainText('CFTR')`
         - Assert population table is visible: `await expect(page.locator('[data-testid="population-table"]')).toBeVisible()`

    **url-sharing.spec.ts — URL State Roundtrip:**

    1. Same API interception setup as cftr-wizard.spec.ts

    2. Test: "URL state roundtrip preserves wizard state"
       - Navigate through the wizard to Step 4 with CFTR (same flow as happy path)
       - Capture the current URL: `const url = page.url()`
       - Assert URL contains gene parameter: `expect(url).toContain('gene=CFTR')`
       - Open a new page in the same browser context: `const freshPage = await page.context().newPage()`
       - Set up same API route interception on freshPage (gnomAD requests will fire again when state restores)
       - Navigate to the captured URL: `await freshPage.goto(url)`
       - Wait for the app to restore state and load data
       - Assert results are visible on the fresh page: `await expect(freshPage.locator('[data-testid="results-summary-card"]')).toBeVisible()`
       - Clean up: `await freshPage.close()`

    3. Run locally to verify:
       ```
       npx playwright test
       ```
       This starts the dev server (if not already running) and runs both spec files.

    IMPORTANT: The exact selectors and interaction patterns MUST be verified by reading the Vue source files. The stepper may require specific interactions (clicking step headers, waiting for transitions). Playwright's `waitForSelector` and `toBeVisible` assertions handle async rendering.

    IMPORTANT: If the wizard uses a `v-stepper` from Vuetify, step transitions may be animated. Use `await page.waitForTimeout(300)` or better, wait for specific elements to appear rather than using fixed timeouts.
  </action>
  <verify>
    - `npx playwright test` runs both spec files and passes
    - CFTR happy path reaches Step 4 with visible results and clinical text containing 'CFTR'
    - URL roundtrip opens fresh page with captured URL and sees restored results
    - No real gnomAD API calls are made (all intercepted by page.route)
  </verify>
  <done>
    2 Playwright E2E test files pass. CFTR wizard completes the full 4-step flow. URL state roundtrip preserves and restores wizard state.
  </done>
</task>

</tasks>

<verification>
- `npx playwright test` passes both spec files
- Tests use mocked API (no network dependency)
- Clinical text assertion verifies content, not just container existence
- URL roundtrip test uses a fresh page (not same page reload)
- Fixtures match actual gnomAD GraphQL response types
</verification>

<success_criteria>
- playwright.config.ts exists at repo root with correct testDir and webServer config
- gnomad-responses.ts exports 3 typed fixture objects for GeneSearch, GeneDetails, GeneVariants
- cftr-wizard.spec.ts passes: full 4-step wizard flow ending with visible clinical text containing 'CFTR'
- url-sharing.spec.ts passes: URL captured after wizard completion, fresh page navigates to URL and sees restored results
- All API calls intercepted via page.route with operationName matching
</success_criteria>

<output>
After completion, create `.planning/phases/29-test-suite-completion/29-04-SUMMARY.md`
</output>
