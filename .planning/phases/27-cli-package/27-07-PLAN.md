---
phase: 27-cli-package
plan: 07
type: execute
wave: 4
depends_on: ["27-04", "27-05", "27-06"]
files_modified:
  - packages/cli/src/__tests__/query.test.ts
  - packages/cli/src/__tests__/batch.test.ts
  - packages/cli/src/__tests__/formatters.test.ts
  - packages/cli/src/__tests__/fixtures/cftr-response.json
  - packages/cli/vitest.config.ts
autonomous: true

must_haves:
  truths:
    - "CLI integration tests pass with mocked gnomAD API responses"
    - "Query command tests verify single gene output for all three formats"
    - "Batch command tests verify multi-gene processing with error handling"
    - "Formatter tests verify text, JSON, and TSV output correctness"
    - "Tests run via bun run test from monorepo root"
  artifacts:
    - path: "packages/cli/src/__tests__/query.test.ts"
      provides: "Query command integration tests with mocked gnomAD responses"
      min_lines: 50
    - path: "packages/cli/src/__tests__/batch.test.ts"
      provides: "Batch command tests for concurrency, error handling, file parsing"
      min_lines: 50
    - path: "packages/cli/src/__tests__/formatters.test.ts"
      provides: "Output formatter unit tests for text, JSON, TSV"
      min_lines: 40
    - path: "packages/cli/src/__tests__/fixtures/cftr-response.json"
      provides: "Mocked gnomAD CFTR API response for deterministic tests"
      min_lines: 20
    - path: "packages/cli/vitest.config.ts"
      provides: "Vitest configuration for CLI package"
      min_lines: 5
  key_links:
    - from: "packages/cli/src/__tests__/query.test.ts"
      to: "packages/cli/src/utils/gene-query.ts"
      via: "queryGene function under test"
      pattern: "import.*queryGene"
    - from: "packages/cli/src/__tests__/formatters.test.ts"
      to: "packages/cli/src/output/text-formatter.ts"
      via: "formatText function under test"
      pattern: "import.*formatText"
---

<objective>
Write integration tests for the CLI with mocked gnomAD API responses, covering single gene query, batch processing, all output formats, and error handling.

Purpose: TEST-08 requires CLI integration tests. Mocked responses ensure deterministic tests that don't depend on gnomAD API availability.
Output: Passing test suite covering all CLI commands and formatters.
</objective>

<execution_context>
@C:\Users\bernt\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\bernt\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/27-cli-package/27-RESEARCH.md
@.planning/phases/27-cli-package/27-04-SUMMARY.md
@.planning/phases/27-cli-package/27-05-SUMMARY.md
@.planning/phases/27-cli-package/27-06-SUMMARY.md
@packages/cli/src/commands/query.ts
@packages/cli/src/commands/batch.ts
@packages/cli/src/utils/gene-query.ts
@packages/cli/src/output/text-formatter.ts
@packages/cli/src/output/json-formatter.ts
@packages/cli/src/output/tsv-formatter.ts
@packages/cli/src/types.ts
@packages/core/src/types/frequency.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Set up test infrastructure and create mock fixtures</name>
  <files>
    packages/cli/vitest.config.ts
    packages/cli/src/__tests__/fixtures/cftr-response.json
    packages/cli/src/__tests__/formatters.test.ts
  </files>
  <action>
1. Create `packages/cli/vitest.config.ts`:
```typescript
import { defineConfig } from 'vitest/config'

export default defineConfig({
  test: {
    include: ['src/__tests__/**/*.test.ts'],
    environment: 'node',
  },
})
```

Ensure the root vitest config picks this up via workspace config. Check the root vitest.config.ts -- if it uses workspace projects, add CLI. If it uses simple include patterns, the packages/cli/vitest.config.ts acts as a workspace project config.

Add a test script to `packages/cli/package.json`:
```json
"test": "vitest run"
```

2. Create mock fixture `packages/cli/src/__tests__/fixtures/cftr-response.json`:

This should be a realistic but minimal gnomAD API response for CFTR. Include:
- 3-5 variants with realistic data (variant_id, pos, ref, alt, exome/genome with population data)
- clinvar_variants array with matching variant_ids
- At least one LoF HC variant and one ClinVar pathogenic variant
- Population data for nfe, asj, afr (at minimum)
- Realistic allele counts and numbers

Structure matching `GeneVariantsResponse`:
```json
{
  "data": {
    "gene": {
      "gene_id": "ENSG00000001626",
      "symbol": "CFTR",
      "variants": [
        {
          "variant_id": "7-117559593-ATCT-A",
          "pos": 117559593,
          "ref": "ATCT",
          "alt": "A",
          "exome": {
            "ac": 1200,
            "an": 250000,
            "ac_hom": 5,
            "populations": [
              { "id": "nfe", "ac": 800, "an": 150000, "ac_hom": 3 },
              { "id": "asj", "ac": 200, "an": 10000, "ac_hom": 1 },
              { "id": "afr", "ac": 50, "an": 40000, "ac_hom": 0 }
            ]
          },
          "genome": {
            "ac": 300,
            "an": 80000,
            "ac_hom": 1,
            "populations": [
              { "id": "nfe", "ac": 200, "an": 50000, "ac_hom": 1 },
              { "id": "asj", "ac": 50, "an": 3000, "ac_hom": 0 },
              { "id": "afr", "ac": 20, "an": 15000, "ac_hom": 0 }
            ]
          },
          "transcript_consequence": {
            "gene_symbol": "CFTR",
            "transcript_id": "ENST00000003084",
            "canonical": true,
            "consequence_terms": ["frameshift_variant"],
            "lof": "HC",
            "lof_filter": null,
            "lof_flags": null,
            "hgvsc": "c.1521_1523delCTT",
            "hgvsp": "p.Phe508del"
          }
        }
        // Add 2-3 more variants with varying characteristics
      ],
      "clinvar_variants": [
        {
          "variant_id": "7-117559593-ATCT-A",
          "clinvar_variation_id": "7105",
          "clinical_significance": "Pathogenic",
          "gold_stars": 4,
          "review_status": "reviewed by expert panel",
          "pos": 117559593,
          "ref": "ATCT",
          "alt": "A"
        }
        // Match variant_ids from variants array
      ]
    }
  }
}
```

Make the fixture realistic enough to test filtering and calculation. Include at least:
- 1 LoF HC variant (should pass filter)
- 1 ClinVar pathogenic variant that's NOT LoF (should pass filter)
- 1 variant that should NOT pass filter (e.g., VUS with low stars)
- Multiple population entries per variant

3. Create `packages/cli/src/__tests__/formatters.test.ts`:

Test all four formatters with a known QueryResult input.

Create a helper function `createMockResult(): QueryResult` that returns a deterministic result object.

Tests:
- `formatText` produces summary blocks with gene header, population sections
- `formatText` output contains "Carrier frequency:" and "Genetic prevalence:"
- `formatText` marks founder effect populations with "[!]"
- `formatJson` produces valid JSON that can be parsed back
- `formatJson` without variants flag omits variants field
- `formatJson` with pretty flag produces indented output
- `formatTsv` produces header row + data rows
- `formatTsv` header contains expected column names
- `formatTsv` properly quotes fields containing special characters
- `formatTsv` array input (batch) produces multiple rows
  </action>
  <verify>
    `cd packages/cli && bun run test` runs formatter tests.
    All formatter tests pass.
  </verify>
  <done>Vitest configured for CLI package. Mock CFTR fixture created. Formatter unit tests pass for text, JSON, and TSV output.</done>
</task>

<task type="auto">
  <name>Task 2: Create query and batch integration tests</name>
  <files>
    packages/cli/src/__tests__/query.test.ts
    packages/cli/src/__tests__/batch.test.ts
  </files>
  <action>
1. Create `packages/cli/src/__tests__/query.test.ts`:

Mock the `executeGraphQLQuery` function from `@gnomad-cf/core/client` to return the CFTR fixture response. Use vitest's `vi.mock`:

```typescript
import { vi, describe, it, expect, beforeEach } from 'vitest'

// Mock the core client
vi.mock('@gnomad-cf/core/client', () => ({
  executeGraphQLQuery: vi.fn(),
}))

import { executeGraphQLQuery } from '@gnomad-cf/core/client'
import { queryGene } from '../utils/gene-query.js'
import cftrFixture from './fixtures/cftr-response.json'
import { FACTORY_FILTER_DEFAULTS, FACTORY_CALC_DEFAULTS } from '@gnomad-cf/core/types'
```

Tests:
- **queryGene returns carrier frequencies for CFTR**: Mock returns fixture, verify result has gene: 'CFTR', populations array is non-empty, globalCarrierFrequency is a number
- **queryGene filters to specific population**: Call with population: 'nfe', verify only nfe population in results
- **queryGene with HWE formula**: Call with calcConfig.useHWEFormula=true, verify result.formula === 'hwe'
- **queryGene with simplified formula**: Call with useHWEFormula=false, verify result.formula === 'simplified'
- **queryGene with homozygote exclusion**: Call with useHomExclusion=true, verify result.homExclusionActive === true
- **queryGene throws on unknown gene**: Mock returns `{ data: { gene: null } }`, expect error "not found"
- **queryGene throws on GraphQL error**: Mock returns `{ errors: [{ message: "bad request" }] }`, expect error
- **queryGene respects filter config**: Call with lofHcEnabled=false, clinvarEnabled=false, verify different variant count vs default
- **Carrier frequency value is reasonable**: For the CFTR fixture with default config, verify carrier frequency is a positive number < 1

2. Create `packages/cli/src/__tests__/batch.test.ts`:

Test the batch file parsing and processing logic. Don't test the full commander command (that requires process.argv manipulation), instead test the core batch functions:

Extract the file parsing logic from batch.ts into a testable function (e.g., `parseGeneListFile(content: string): string[]` or similar). If the logic is tightly coupled to the command handler, refactor it into a separate utility function.

Tests:
- **parseGeneListFile parses plain text (one gene per line)**: Input "CFTR\nHEXA\nGJB2\n", expect ['CFTR', 'HEXA', 'GJB2']
- **parseGeneListFile parses JSON string array**: Input '["CFTR", "HEXA"]', expect ['CFTR', 'HEXA']
- **parseGeneListFile parses JSON object array**: Input '[{"gene":"CFTR"},{"gene":"HEXA"}]', expect gene list
- **parseGeneListFile skips empty lines and comments**: Input "CFTR\n\n# comment\nHEXA\n", expect ['CFTR', 'HEXA']
- **Batch processing collects results**: Mock queryGene, process 3 genes, verify 3 results
- **Batch processing handles failures**: Mock queryGene to throw for one gene, verify 2 results + 1 error
- **Batch processing respects concurrency**: Mock queryGene with delay, verify max concurrent calls equals limit
- **Fail-fast mode stops after first error**: Mock to fail on second gene, verify third gene not attempted

For concurrency testing, use a counter that tracks active calls:
```typescript
let activeCalls = 0
let maxActiveCalls = 0
const mockQueryGene = vi.fn(async (gene: string) => {
  activeCalls++
  maxActiveCalls = Math.max(maxActiveCalls, activeCalls)
  await new Promise(r => setTimeout(r, 50))
  activeCalls--
  return createMockResult(gene)
})
```

3. After creating tests, run:
   - `cd packages/cli && bun run test`
   - `bun run test` (root level, should include CLI tests)

Ensure all tests pass. Fix any type errors or import issues.
  </action>
  <verify>
    `cd packages/cli && bun run test` passes all tests.
    `bun run test` at monorepo root includes CLI tests.
    Test count: at minimum 15 tests across query, batch, and formatter test files.
  </verify>
  <done>CLI integration tests pass with mocked gnomAD responses. Query tests cover single gene, population filter, formula options, and error handling. Batch tests cover file parsing, concurrency, error collection, and fail-fast. Addresses TEST-08.</done>
</task>

</tasks>

<verification>
- `bun run test` at root runs CLI tests alongside core tests
- All CLI tests pass (query, batch, formatters)
- Mock fixture provides deterministic test data
- Tests cover: single gene query, population filtering, format flags, batch file parsing, concurrency, error handling, fail-fast
- No flaky tests (all mocked, no network calls)
</verification>

<success_criteria>
- TEST-08: CLI integration tests with mocked gnomAD responses pass
- Tests cover single gene mode, batch mode, all format flags, and error handling edge cases
- Tests are deterministic (mocked API, no real network calls)
- `bun run test` at monorepo root includes CLI tests in its run
</success_criteria>

<output>
After completion, create `.planning/phases/27-cli-package/27-07-SUMMARY.md`
</output>
