---
phase: 06-app-shell
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - public/favicon.svg
  - public/favicon.png
  - public/apple-touch-icon.png
  - src/main.ts
  - index.html
autonomous: true

must_haves:
  truths:
    - "Browser tab displays custom gCFC favicon"
    - "Favicon adapts to dark/light browser theme"
    - "Theme colors reflect RequiForm-inspired palette"
  artifacts:
    - path: "public/favicon.svg"
      provides: "Primary favicon with dark mode CSS support"
      contains: "gCFC"
    - path: "public/favicon.png"
      provides: "PNG fallback favicon 32x32"
    - path: "public/apple-touch-icon.png"
      provides: "iOS touch icon 180x180"
    - path: "src/main.ts"
      provides: "Vuetify theme with RequiForm colors"
      contains: "#a09588"
    - path: "index.html"
      provides: "Favicon link elements"
      contains: "favicon.svg"
  key_links:
    - from: "index.html"
      to: "public/favicon.svg"
      via: "link rel=icon"
      pattern: 'rel="icon".*favicon\\.svg'
---

<objective>
Create custom branded favicon with dark mode support and update Vuetify theme colors to RequiForm-inspired palette.

Purpose: Establish visual brand identity with consistent favicon and theme colors before UI component updates.
Output: Favicon files (SVG, PNG, iOS), updated theme configuration, updated HTML head.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-app-shell/06-CONTEXT.md
@.planning/phases/06-app-shell/06-RESEARCH.md
@src/main.ts
@index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create favicon assets with dark mode support</name>
  <files>public/favicon.svg, public/favicon.png, public/apple-touch-icon.png</files>
  <action>
    Create three favicon files:

    1. **public/favicon.svg** - Primary favicon with CSS dark mode:
       - SVG 32x32 viewBox
       - Text "gCFC" centered
       - Font: system-ui, bold, ~10px
       - Light mode fill: #424242 (secondary color)
       - Dark mode fill: #E0E0E0 (via @media prefers-color-scheme: dark)
       - Use dominant-baseline="central" and text-anchor="middle" for centering

    2. **public/favicon.png** - 32x32 PNG fallback:
       - Generate from SVG concept (dark text on transparent)
       - Use #424242 text color
       - Can be created via canvas or image tool

    3. **public/apple-touch-icon.png** - 180x180 for iOS:
       - Larger version with padding
       - Light background (#FAFAFA) for iOS visibility
       - #424242 text

    NOTE: For PNG generation, if image tools unavailable, create placeholder files and document that manual creation is needed post-plan.
  </action>
  <verify>
    ls -la public/favicon.* public/apple-touch-icon.png
    File sizes: favicon.svg > 200 bytes, favicon.png ~500 bytes, apple-touch-icon.png ~1KB
    grep "gCFC" public/favicon.svg returns match
    grep "prefers-color-scheme" public/favicon.svg returns match
  </verify>
  <done>
    - favicon.svg exists with gCFC text and dark mode CSS
    - favicon.png exists as 32x32 fallback
    - apple-touch-icon.png exists as 180x180 iOS icon
  </done>
</task>

<task type="auto">
  <name>Task 2: Update theme colors and favicon references</name>
  <files>src/main.ts, index.html</files>
  <action>
    1. **src/main.ts** - Update Vuetify theme configuration:
       - Light theme primary: '#a09588' (RequiForm muted taupe)
       - Light theme secondary: '#424242' (dark gray)
       - Light theme surface: '#FFFFFF'
       - Light theme background: '#FAFAFA'
       - Dark theme primary: '#BDBDBD' (lighter for visibility)
       - Dark theme secondary: '#757575' (medium gray)
       - Keep existing structure, only update color values

    2. **index.html** - Update favicon links in head:
       - Remove existing vite.svg link
       - Add: link rel="icon" href="./favicon.svg" type="image/svg+xml"
       - Add: link rel="icon" href="./favicon.png" type="image/png" sizes="32x32"
       - Add: link rel="apple-touch-icon" href="./apple-touch-icon.png"
       - Order: SVG first (modern browsers), PNG fallback, Apple touch icon
       - Use ./ prefix for paths (Vite will resolve correctly)
  </action>
  <verify>
    bun run typecheck passes
    grep "#a09588" src/main.ts returns match
    grep "favicon.svg" index.html returns match
    grep "apple-touch-icon" index.html returns match
    bun run build succeeds
  </verify>
  <done>
    - Vuetify light theme uses #a09588 primary
    - Vuetify dark theme uses #BDBDBD primary
    - index.html references all three favicon formats
    - Build passes without errors
  </done>
</task>

</tasks>

<verification>
After both tasks:
1. bun run build - succeeds
2. bun run dev - app runs at localhost:5173
3. Browser tab shows "gCFC" favicon (may need cache clear)
4. In dev tools, verify favicon.svg loads successfully
5. Theme colors visible in app bar (subtle change from blue to taupe)
</verification>

<success_criteria>
1. Custom favicon visible in browser tab
2. Favicon adapts to dark/light system preference (SVG version)
3. iOS devices can add to home screen with proper icon
4. Theme colors updated to RequiForm-inspired palette
5. Build passes, app runs without errors
</success_criteria>

<output>
After completion, create `.planning/phases/06-app-shell/06-01-SUMMARY.md`
</output>
