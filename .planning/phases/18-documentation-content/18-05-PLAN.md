---
phase: 18-documentation-content
plan: 05
type: execute
wave: 3
depends_on: ["18-01", "18-02", "18-03", "18-04"]
files_modified:
  - docs/index.md
autonomous: true

must_haves:
  truths:
    - "Landing page includes a research-use-only disclaimer"
    - "All cross-links between pages resolve correctly in the built site"
    - "All screenshot embeds render correctly in the VitePress build"
    - "VitePress build completes with zero errors and zero broken links"
    - "No placeholder text remains on any documentation page"
  artifacts:
    - path: "docs/index.md"
      provides: "Updated landing page with disclaimer"
      contains: "research"
  key_links:
    - from: "docs/guide/getting-started.md"
      to: "/use-cases/carrier-screening"
      via: "markdown link"
      pattern: "use-cases/carrier-screening"
    - from: "docs/use-cases/carrier-screening.md"
      to: "/reference/filters"
      via: "markdown link"
      pattern: "reference/filters"
    - from: "docs/reference/templates.md"
      to: "/use-cases/clinical-letter"
      via: "markdown link"
      pattern: "use-cases/clinical-letter"
---

<objective>
Update the landing page with a research disclaimer and perform a comprehensive build verification across all documentation pages to catch broken links, missing screenshots, and remaining placeholder text.

Purpose: This is the final quality gate for Phase 18. All content pages are written by Plans 01-04. This plan ensures everything works together: cross-links resolve, screenshots render, and the landing page includes the required research disclaimer.

Output: Updated docs/index.md, verified build output with no errors or broken references.
</objective>

<execution_context>
@C:\Users\bernt\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\bernt\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-documentation-content/18-CONTEXT.md
@.planning/phases/18-documentation-content/18-01-SUMMARY.md
@.planning/phases/18-documentation-content/18-02-SUMMARY.md
@.planning/phases/18-documentation-content/18-03-SUMMARY.md
@.planning/phases/18-documentation-content/18-04-SUMMARY.md
@docs/index.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update landing page with research disclaimer</name>
  <files>docs/index.md</files>
  <action>
Update `docs/index.md` to add a research-use-only disclaimer in the content area below the frontmatter features section. The landing page uses VitePress `layout: home` with hero and features in YAML frontmatter. Add a markdown content section AFTER the closing `---` of the frontmatter.

The existing content after `---` is:
"A research tool for genetic counselors and clinical researchers to calculate carrier frequencies for autosomal recessive conditions using population data from the Genome Aggregation Database (gnomAD)."

Replace/expand this to:

```markdown
A research tool for clinical researchers and genetic counselors to calculate carrier frequencies for autosomal recessive conditions using population data from the Genome Aggregation Database (gnomAD).

::: warning For Research Use Only
The gnomAD Carrier Frequency Calculator is intended to support clinical documentation workflows and research. Results should be reviewed by a qualified clinician and do not constitute a diagnostic report. Always verify calculations against primary sources before clinical use.
:::
```

Do NOT modify the hero or features frontmatter — they are correct as-is from Phase 16. Only update the content section after `---`.
  </action>
  <verify>Run `npx vitepress build docs` to verify the landing page builds correctly. Inspect the built HTML for the warning container.</verify>
  <done>Landing page includes research-use-only disclaimer visible below the hero and features sections.</done>
</task>

<task type="auto">
  <name>Task 2: Comprehensive build verification and cross-link audit</name>
  <files></files>
  <action>
Run a comprehensive verification of the entire documentation site:

1. **Build check**: Run `npx vitepress build docs` and verify it completes with zero errors.

2. **Placeholder audit**: Search all docs `.md` files for the placeholder text "under construction" or "will be added in a future update". NONE should remain. If any are found, fix them by replacing with appropriate content or removing the placeholder sentence.

3. **Screenshot audit**: For every `<figure class="screenshot-frame">` in docs markdown files, verify the referenced screenshot file exists in `docs/public/screenshots/`. The 14 valid screenshots are:
   - step-1-gene-search.webp
   - step-1-gene-selected.webp
   - step-2-patient-status.webp
   - step-3-frequency.webp
   - step-4-results.webp
   - text-output.webp
   - variant-table.webp
   - filter-chips.webp
   - settings-dialog.webp
   - hero-preview.webp
   - dark-mode-results.webp
   - mobile-results.webp
   - population-drilldown.webp
   - search-history.webp

4. **Cross-link audit**: Search all docs `.md` files for markdown links (pattern: `](/...)`). For each internal link, verify the target page exists. Internal links should use absolute paths from docs root (e.g., `/guide/getting-started`, NOT `./getting-started.md`). Key links to verify:
   - guide/ pages link to use-cases/ and reference/
   - use-cases/ pages link to reference/ pages
   - reference/ pages link to use-cases/ for examples
   - about/ pages link to guide/ for getting started

5. **Contributing sidebar**: Verify that `docs/.vitepress/config.ts` includes `{ text: 'Contributing', link: '/about/contributing' }` in the /about/ sidebar.

6. **CITATION.cff**: Verify `CITATION.cff` exists at repo root and has required fields (cff-version, title, authors, version).

If ANY issues are found, fix them in-place. This task is a verification + fix task, not just a check.
  </action>
  <verify>
Final build: `npx vitepress build docs` completes with 0 errors.
Placeholder check: `grep -r "under construction" docs/*.md docs/**/*.md` returns empty.
Screenshot check: All referenced screenshot paths resolve to existing files.
  </verify>
  <done>VitePress build succeeds with zero errors. No placeholder text remains. All screenshot references are valid. All cross-links point to existing pages. CITATION.cff exists. Contributing is in sidebar. The documentation site is complete and ready for deployment.</done>
</task>

</tasks>

<verification>
1. `npx vitepress build docs` completes with zero errors
2. No "under construction" or "future update" text in any docs .md file
3. All screenshot `src="/screenshots/..."` paths reference existing files in docs/public/screenshots/
4. All internal cross-links point to existing documentation pages
5. docs/index.md includes research disclaimer
6. CITATION.cff exists at repo root
7. config.ts sidebar includes Contributing link under /about/
</verification>

<success_criteria>
- Landing page has research disclaimer (from CONTEXT.md decisions)
- Zero broken links across all documentation pages
- Zero missing screenshot references
- Zero remaining placeholder text
- VitePress build completes cleanly
- The complete documentation site is ready for Phase 19 (CI/CD deployment)
</success_criteria>

<output>
After completion, create `.planning/phases/18-documentation-content/18-05-SUMMARY.md`
</output>
