---
phase: 22-cta-color-accessibility
plan: 02
type: execute
wave: 2
depends_on: ["22-01"]
files_modified:
  - src/components/FilterChips.vue
  - src/components/FilterPanel.vue
  - src/components/HistoryPanel.vue
  - src/components/TemplateEditor.vue
  - src/components/VariablePicker.vue
  - src/components/DataSourcesDialog.vue
  - src/components/AboutDialog.vue
  - src/components/SettingsDialog.vue
autonomous: true

must_haves:
  truths:
    - "Non-CTA elements (filter chips, switches, decorative icons, variable chips) use secondary color instead of primary"
    - "CTA buttons (Continue, Save, Install, Close) still use primary (teal)"
    - "Semantic colors (success, warning, error on filter chips) are preserved unchanged"
  artifacts:
    - path: "src/components/FilterChips.vue"
      provides: "LoF HC chip uses secondary color"
      contains: 'color="secondary"'
    - path: "src/components/FilterPanel.vue"
      provides: "LoF HC filter switch uses secondary color"
      contains: 'color="secondary"'
    - path: "src/components/SettingsDialog.vue"
      provides: "LoF HC switch uses secondary, CTA buttons keep primary"
      contains: 'color="secondary"'
  key_links:
    - from: "color='primary' audit"
      to: "8 bindings changed to secondary"
      via: "template color prop"
      pattern: 'color="secondary"'
---

<objective>
Audit all `color="primary"` bindings across the codebase and migrate non-CTA uses to `color="secondary"` (warm gray). CTA buttons, active states, and progress indicators keep `color="primary"` (teal).

Purpose: Ensure teal is reserved for interactive CTAs while warm gray handles passive/decorative elements, maintaining clear visual hierarchy.
Output: 8 bindings changed from primary to secondary; 15 bindings confirmed as correctly primary.
</objective>

<execution_context>
@C:\Users\bernt\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\bernt\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/22-cta-color-accessibility/22-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate non-CTA color="primary" to color="secondary"</name>
  <files>
    src/components/FilterChips.vue,
    src/components/FilterPanel.vue,
    src/components/HistoryPanel.vue,
    src/components/TemplateEditor.vue,
    src/components/VariablePicker.vue,
    src/components/DataSourcesDialog.vue,
    src/components/AboutDialog.vue,
    src/components/SettingsDialog.vue
  </files>
  <action>
Change `color="primary"` to `color="secondary"` in exactly these 8 locations:

1. **FilterChips.vue line ~8** — `v-chip` for LoF HC filter chip. This is an informational indicator, not a CTA. Change to `color="secondary"`.

2. **FilterPanel.vue line ~26** — `v-switch` for LoF HC filter toggle. This is a toggle control, not a CTA. Change to `color="secondary"`.

3. **HistoryPanel.vue line ~71** — `v-icon` with DNA icon in history list entries. This is a decorative icon, not a CTA. Change to `color="secondary"`.

4. **TemplateEditor.vue line ~53** — `v-chip` for variable name chips in the template editor. These are informational labels, not CTAs. Change to `color="secondary"`.

5. **VariablePicker.vue line ~36** — `v-chip` for variable name chips in the picker. These are informational labels, not CTAs. Change to `color="secondary"`.

6. **DataSourcesDialog.vue line ~58** — `v-chip` showing selected gnomAD version. This is an informational indicator, not a CTA. Change to `color="secondary"`.

7. **AboutDialog.vue line ~36** — `v-icon` with DNA decorative icon (mdi-dna, size 64). This is a decorative element, not a CTA. Change to `color="secondary"`.

8. **SettingsDialog.vue line ~435** — `v-switch` for LoF HC filter toggle in settings. This is a toggle control, not a CTA. Change to `color="secondary"`.

**Do NOT change these bindings (they are correct CTAs/active states):**
- App.vue: v-progress-circular (loading), v-snackbar (notification)
- DisclaimerBanner.vue: "I Understand" button
- SettingsDialog.vue: Install button (~389), language btn-toggle (~503), Save button (~577)
- VariantModal.vue: Close button
- StepFrequency.vue: tabs bg-color, loading spinner, Continue button
- StepGene.vue: Continue button
- StepStatus.vue: Continue button
- StepResults.vue: "View all variants" button, "Start Over" button
- TextOutput.vue: perspective toggle
- FrequencyResults.vue: loading spinner

**Do NOT change semantic colors** (success, warning, error on filter chips in FilterChips.vue lines 18+ and VariantTable.vue). These already use correct semantic colors.
  </action>
  <verify>
Run `grep -rn 'color="primary"' src/components/FilterChips.vue src/components/FilterPanel.vue src/components/HistoryPanel.vue src/components/TemplateEditor.vue src/components/VariablePicker.vue src/components/DataSourcesDialog.vue src/components/AboutDialog.vue` — should return zero matches.

Run `grep -rn 'color="secondary"' src/components/FilterChips.vue src/components/FilterPanel.vue src/components/HistoryPanel.vue src/components/TemplateEditor.vue src/components/VariablePicker.vue src/components/DataSourcesDialog.vue src/components/AboutDialog.vue` — should find the 7 newly changed bindings (plus any pre-existing secondary uses).

For SettingsDialog.vue: `grep -c 'color="primary"' src/components/SettingsDialog.vue` should return 3 (Install, language toggle, Save — the CTA ones). `grep -c 'color="secondary"' src/components/SettingsDialog.vue` should return 2 (the switch, plus the pre-existing one at ~445).

Run `bun run typecheck` to confirm no type errors.
  </verify>
  <done>
8 non-CTA `color="primary"` bindings changed to `color="secondary"`. All CTA buttons and active states still use `color="primary"` (teal). Semantic colors (success/warning/error) untouched.
  </done>
</task>

</tasks>

<verification>
1. `bun run typecheck` passes
2. `bun run build` succeeds
3. Total `color="primary"` count across src/ is 16 (down from 24; 8 migrated to secondary)
4. The 8 migrated elements use `color="secondary"` (warm gray)
5. No semantic colors (success, warning, error) were modified
</verification>

<success_criteria>
Non-CTA elements render in warm gray secondary. CTA buttons render in teal primary. Visual hierarchy is clear: teal = interactive action, warm gray = passive/informational. Build passes.
</success_criteria>

<output>
After completion, create `.planning/phases/22-cta-color-accessibility/22-02-SUMMARY.md`
</output>
