---
phase: 22-cta-color-accessibility
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main.ts
  - vite.config.ts
  - index.html
autonomous: true

must_haves:
  truths:
    - "Primary CTA buttons render in teal (#117A7F light / #4DB6AC dark) instead of warm gray"
    - "Warm gray #a09588 is available as color='secondary' in templates"
    - "PWA manifest theme_color is #117A7F"
    - "Seed content in index.html uses teal for CTA button and header background"
  artifacts:
    - path: "src/main.ts"
      provides: "Vuetify theme with teal primary and warm gray secondary"
      contains: "#117A7F"
    - path: "vite.config.ts"
      provides: "PWA manifest with updated theme_color"
      contains: "#117A7F"
    - path: "index.html"
      provides: "Seed content CSS updated from #a09588 to #117A7F"
  key_links:
    - from: "src/main.ts"
      to: "all components using color='primary'"
      via: "Vuetify theme system"
      pattern: "primary: '#117A7F'"
---

<objective>
Replace the Vuetify primary color from warm gray `#a09588` to teal `#117A7F` (light) and `#4DB6AC` (dark), assign warm gray as `secondary`, and update the PWA manifest and seed content inline CSS.

Purpose: The current warm gray primary makes CTA buttons look disabled. Teal provides WCAG AA contrast (5.10:1 light, 6.83:1 dark) and communicates "clickable" to users.
Output: Theme colors changed at the source, all CTA elements automatically render in teal.
</objective>

<execution_context>
@C:\Users\bernt\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\bernt\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/main.ts
@vite.config.ts
@index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Vuetify theme colors and PWA manifest</name>
  <files>src/main.ts, vite.config.ts</files>
  <action>
In `src/main.ts`, update the `createVuetify` theme configuration:

**Light theme:**
- `primary`: change from `'#a09588'` to `'#117A7F'` (teal CTA)
- `secondary`: change from `'#424242'` to `'#a09588'` (warm gray brand)
- Keep `surface` and `background` unchanged

**Dark theme:**
- `primary`: change from `'#BDBDBD'` to `'#4DB6AC'` (Material Teal 300)
- `secondary`: change from `'#757575'` to `'#a09588'` (warm gray, same in both themes)

In `vite.config.ts`, update the PWA manifest:
- `theme_color`: change from `'#a09588'` to `'#117A7F'`
- Update the comment from `// RequiForm palette` to `// Teal CTA palette`

Do NOT touch any component files — the Vuetify theme system propagates automatically.
  </action>
  <verify>
Run `bun run typecheck` to confirm no type errors.
Grep `src/main.ts` for `#117A7F` and `#4DB6AC` to confirm both theme colors.
Grep `vite.config.ts` for `#117A7F` to confirm manifest update.
  </verify>
  <done>
Vuetify light primary is `#117A7F`, dark primary is `#4DB6AC`, secondary is `#a09588` in both themes, PWA manifest theme_color is `#117A7F`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update seed content CSS in index.html</name>
  <files>index.html</files>
  <action>
The seed content (pre-Vue-mount HTML) has hardcoded `#a09588` in three CSS rules. Update these to the new teal primary:

1. `.seed-header` background: change `#a09588` to `#117A7F`
2. `.seed-features li` border-left: change `#a09588` to `#117A7F`
3. `.seed-cta` background: change `#a09588` to `#117A7F`
4. `.seed-cta:hover` background: change `#8a7e73` to `#0D5F63` (teal darkened ~15% for hover)

These are the ONLY four `#a09588`/`#8a7e73` occurrences in the seed CSS. The rest of the seed colors (`#5a4e46` headings, `#f8f5f3` backgrounds, `#e8e2de` borders) are neutral/warm gray tones for body text and remain unchanged.

Do NOT update colors in the structured data JSON or meta tags — those are content descriptions, not visual styles.
  </action>
  <verify>
Grep `index.html` for `#a09588` — should return zero matches in CSS (may still appear in non-CSS contexts like structured data text).
Grep `index.html` for `#117A7F` — should find 3 occurrences (header, border-left, CTA button).
  </verify>
  <done>
Seed content CTA button, header bar, and feature list borders all render in teal `#117A7F` before Vue mounts.
  </done>
</task>

</tasks>

<verification>
1. `bun run typecheck` passes
2. `bun run build` succeeds
3. `grep -c "#117A7F" src/main.ts` returns 1
4. `grep -c "#4DB6AC" src/main.ts` returns 1
5. `grep -c "#a09588" src/main.ts` returns 1 (secondary color)
6. `grep -c "#117A7F" vite.config.ts` returns 1
7. Seed CSS in index.html has no `#a09588` references
</verification>

<success_criteria>
Theme colors are defined: teal primary (#117A7F/#4DB6AC), warm gray secondary (#a09588). PWA manifest uses teal. Seed content CSS uses teal. Build passes.
</success_criteria>

<output>
After completion, create `.planning/phases/22-cta-color-accessibility/22-01-SUMMARY.md`
</output>
