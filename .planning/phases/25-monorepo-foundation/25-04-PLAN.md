---
phase: 25-monorepo-foundation
plan: 04
type: execute
wave: 4
depends_on: ["25-03"]
files_modified:
  - packages/core/src/client/index.ts
  - packages/core/src/index.ts
  - apps/web/src/composables/useGeneSearch.ts
  - apps/web/src/composables/useGeneVariants.ts
  - apps/web/src/composables/useCarrierFrequency.ts
  - apps/web/src/composables/useTextGenerator.ts
  - apps/web/src/composables/useVariantFilters.ts
  - apps/web/src/composables/useClinvarSubmissions.ts
  - apps/web/src/composables/useExclusionState.ts
  - apps/web/src/composables/useExport.ts
  - apps/web/src/composables/useUrlState.ts
  - apps/web/src/composables/useHistoryRestore.ts
  - apps/web/src/composables/useHistoryAutoSave.ts
  - apps/web/src/composables/useLogger.ts
  - apps/web/src/composables/index.ts
  - apps/web/src/stores/useFilterStore.ts
  - apps/web/src/stores/useTemplateStore.ts
  - apps/web/src/stores/useAppStore.ts
  - apps/web/src/stores/useHistoryStore.ts
  - apps/web/src/stores/useLogStore.ts
  - apps/web/src/stores/useClingenStore.ts
  - apps/web/src/components/**
  - apps/web/src/api/client.ts
  - apps/web/src/api/index.ts
autonomous: true

must_haves:
  truths:
    - "Web composables import types from @gnomad-cf/core/types instead of @/types"
    - "Web composables import query strings from @gnomad-cf/core/queries instead of @/api/queries"
    - "Web composables import calculation functions from @gnomad-cf/core/calculations instead of @/utils"
    - "Web composables import filter functions from @gnomad-cf/core/filters instead of @/utils"
    - "Web composables import template functions from @gnomad-cf/core/templates instead of @/utils"
    - "Web composables import config from @gnomad-cf/core/config instead of @/config"
    - "The old apps/web/src/types/, apps/web/src/config/, and apps/web/src/utils/ directories are deleted"
    - "The fetch-based core client exists at @gnomad-cf/core/client"
  artifacts:
    - path: "packages/core/src/client/index.ts"
      provides: "fetch-based gnomAD GraphQL client"
      contains: "executeGraphQLQuery"
    - path: "apps/web/src/composables/useGeneSearch.ts"
      provides: "Vue wrapper for gene search"
      contains: "@gnomad-cf/core"
    - path: "apps/web/src/composables/useCarrierFrequency.ts"
      provides: "Vue wrapper for carrier frequency"
      contains: "@gnomad-cf/core"
  key_links:
    - from: "apps/web/src/composables/useGeneSearch.ts"
      to: "packages/core/src/queries"
      via: "import GENE_SEARCH_QUERY"
      pattern: "from.*@gnomad-cf/core/queries"
    - from: "apps/web/src/composables/useCarrierFrequency.ts"
      to: "packages/core/src/calculations"
      via: "import calculateCarrierFrequency"
      pattern: "from.*@gnomad-cf/core/calculations"
    - from: "apps/web/src/composables/useVariantFilters.ts"
      to: "packages/core/src/filters"
      via: "import filter functions"
      pattern: "from.*@gnomad-cf/core/filters"
    - from: "apps/web/src/composables/useTextGenerator.ts"
      to: "packages/core/src/templates"
      via: "import render functions"
      pattern: "from.*@gnomad-cf/core/templates"
    - from: "packages/core/src/client/index.ts"
      to: "packages/core/src/config"
      via: "import getApiEndpoint"
      pattern: "from.*config"
---

<objective>
Create the fetch-based core client, rewire all web app imports from local paths to @gnomad-cf/core/*, and delete the duplicated source files from apps/web/.

Purpose: Complete the core extraction by creating the standalone GraphQL client and migrating every web app import to use the core package. After this plan, apps/web/src/ only contains Vue-specific code (composables, stores, components, api/client.ts with villus).

Output: Fully wired monorepo where web app imports from @gnomad-cf/core/* and local duplicates are removed.
</objective>

<execution_context>
@C:\Users\bernt\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\bernt\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-monorepo-foundation/25-CONTEXT.md
@.planning/phases/25-monorepo-foundation/25-RESEARCH.md
@.planning/phases/25-monorepo-foundation/25-03-SUMMARY.md

# Files to update
@apps/web/src/composables/index.ts
@apps/web/src/composables/useGeneSearch.ts
@apps/web/src/composables/useGeneVariants.ts
@apps/web/src/composables/useCarrierFrequency.ts
@apps/web/src/composables/useTextGenerator.ts
@apps/web/src/composables/useVariantFilters.ts
@apps/web/src/composables/useClinvarSubmissions.ts
@apps/web/src/composables/useExclusionState.ts
@apps/web/src/composables/useExport.ts
@apps/web/src/composables/useUrlState.ts
@apps/web/src/composables/useHistoryRestore.ts
@apps/web/src/composables/useHistoryAutoSave.ts
@apps/web/src/composables/useLogger.ts
@apps/web/src/stores/useFilterStore.ts
@apps/web/src/stores/useTemplateStore.ts
@apps/web/src/stores/useAppStore.ts
@apps/web/src/stores/useHistoryStore.ts
@apps/web/src/stores/useLogStore.ts
@apps/web/src/stores/useClingenStore.ts
@apps/web/src/api/client.ts
@apps/web/src/api/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create fetch-based GraphQL client in core</name>
  <files>
    packages/core/src/client/index.ts
    packages/core/src/index.ts
  </files>
  <action>
  **Step 1 — Create packages/core/src/client/index.ts:**
  Implement a pure fetch-based GraphQL client as specified in RESEARCH.md:

  ```typescript
  import { getApiEndpoint } from '../config/index.js'
  import type { GnomadVersion } from '../config/types.js'

  export interface GraphQLRequest {
    query: string
    variables?: Record<string, unknown>
  }

  export interface GraphQLResponse<T> {
    data?: T
    errors?: Array<{ message: string }>
  }

  export async function executeGraphQLQuery<T>(
    request: GraphQLRequest,
    version?: GnomadVersion
  ): Promise<GraphQLResponse<T>> {
    const endpoint = getApiEndpoint(version)
    const response = await fetch(endpoint, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify(request),
    })
    if (!response.ok) {
      throw new Error(`GraphQL request failed: ${response.status}`)
    }
    return response.json() as Promise<GraphQLResponse<T>>
  }
  ```

  This uses standard `fetch()` (available in Node 18+, Bun, and browsers). No villus. No Vue.

  **Step 2 — Update packages/core/src/index.ts barrel:**
  Add client re-export:
  ```typescript
  export * from './client/index.js'
  ```

  **Step 3 — Verify core builds:**
  ```bash
  cd packages/core && bun run build
  ```
  </action>
  <verify>
  - `cd packages/core && npx tsc --noEmit` passes
  - `cd packages/core && bun run build` succeeds
  - packages/core/dist/client.js exists in build output
  - No 'vue', 'villus', or 'pinia' imports in packages/core/src/client/
  </verify>
  <done>
  Fetch-based GraphQL client exists at packages/core/src/client/ and builds correctly. It uses standard fetch(), works in Node, Bun, and browser environments.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewire all web app imports to @gnomad-cf/core/* and delete duplicates</name>
  <files>
    apps/web/src/composables/*.ts
    apps/web/src/stores/*.ts
    apps/web/src/components/**/*.ts
    apps/web/src/components/**/*.vue
    apps/web/src/api/client.ts
    apps/web/src/api/index.ts
  </files>
  <action>
  This is the most critical task in the extraction. Systematically find and replace ALL imports that point to the old local paths with @gnomad-cf/core/* imports.

  **Import mapping rules:**
  | Old import pattern | New import |
  |---|---|
  | `from '@/types'` or `from '@/types/*'` | `from '@gnomad-cf/core/types'` |
  | `from '@/config'` or `from '@/config/*'` | `from '@gnomad-cf/core/config'` |
  | `from '@/api/queries'` or `from '@/api/queries/*'` | `from '@gnomad-cf/core/queries'` |
  | `from '@/utils/variant-filters'` | `from '@gnomad-cf/core/filters'` |
  | `from '@/utils/frequency-calc'` | `from '@gnomad-cf/core/calculations'` |
  | `from '@/utils/template-renderer'` | `from '@gnomad-cf/core/templates'` |
  | `from '@/utils/template-parser'` | `from '@gnomad-cf/core/templates'` |
  | `from '@/utils/formatters'` | `from '@gnomad-cf/core/calculations'` (or wherever formatters went) |
  | `from '@/utils/variant-display'` | `from '@gnomad-cf/core/filters'` (or wherever it went) |
  | `from '@/utils/exclusion-url'` | `from '@gnomad-cf/core/types'` (or wherever it went) |
  | `from '@/utils/export-utils'` | `from '@gnomad-cf/core/...'` (wherever it went) |

  Imports that STAY as `@/` (do NOT change):
  | Import | Reason |
  |---|---|
  | `from '@/composables/*'` | Vue composables stay in web |
  | `from '@/stores/*'` | Pinia stores stay in web |
  | `from '@/components/*'` | Vue components stay in web |
  | `from '@/api/client'` | villus client stays in web |
  | `from '@/App.vue'` | Vue root stays in web |

  **Step 1 — Update composables one by one:**
  Read each composable file. For every import statement:
  - If it imports from `@/types`, `@/config`, `@/api/queries`, `@/utils/*` — rewrite to the appropriate `@gnomad-cf/core/*` subpath
  - If it imports from `@/composables/*`, `@/stores/*`, `@/api/client` — keep as `@/`

  Key composables to update:
  - `useGeneSearch.ts`: imports query strings from queries, types from types
  - `useGeneVariants.ts`: imports query strings from queries, types from types
  - `useCarrierFrequency.ts`: imports frequency-calc from calculations, types from types, config from config
  - `useTextGenerator.ts`: imports template-renderer from templates, types from types
  - `useVariantFilters.ts`: imports variant-filters from filters, types from types
  - `useClinvarSubmissions.ts`: imports clinvar query from queries, types from types
  - `useExclusionState.ts`: imports types, config
  - `useExport.ts`: imports export-utils, types, config
  - `useUrlState.ts`: imports url-state types, types
  - `useHistoryRestore.ts`: imports types, config
  - `useHistoryAutoSave.ts`: imports types
  - `useLogger.ts`: imports types

  **Step 2 — Update stores:**
  Same process for Pinia stores. They import types and config — rewrite to core.

  **Step 3 — Update components:**
  Scan all `.vue` and `.ts` files in apps/web/src/components/. Any that import from `@/types`, `@/config`, `@/utils/*`, or `@/api/queries` need updating.

  Use a systematic approach:
  ```bash
  grep -r "from '@/types" apps/web/src/components/
  grep -r "from '@/config" apps/web/src/components/
  grep -r "from '@/utils" apps/web/src/components/
  grep -r "from '@/api/queries" apps/web/src/components/
  ```
  Update each match.

  **Step 4 — Update apps/web/src/api/client.ts:**
  This file stays in web (it uses villus). But check if it imports types from `@/types` or config from `@/config` — rewrite those to `@gnomad-cf/core/types` and `@gnomad-cf/core/config`.

  **Step 5 — Update apps/web/src/api/index.ts:**
  Check if it re-exports from queries — if so, update to re-export from `@gnomad-cf/core/queries` or remove the re-export (no longer needed since consumers import from core directly).

  **Step 6 — Delete duplicate source files from web:**
  After ALL imports are rewired:
  ```bash
  rm -rf apps/web/src/types/
  rm -rf apps/web/src/config/
  rm -rf apps/web/src/utils/
  rm -rf apps/web/src/api/queries/
  ```
  Keep apps/web/src/api/client.ts and apps/web/src/api/index.ts (villus client).

  **Step 7 — Verify no broken imports:**
  ```bash
  cd apps/web && npx vue-tsc --noEmit
  ```
  This runs the full TypeScript check for the web app. Every import must resolve.

  **Step 8 — Verify full build:**
  ```bash
  bun run build
  ```
  Both core and web must build successfully.

  **Step 9 — Verify dev server:**
  ```bash
  bun run dev
  ```
  Start the dev server, check that HMR works and the page loads without console errors. The Vite alias should resolve `@gnomad-cf/core/*` to packages/core/src/ for hot-reload.
  </action>
  <verify>
  - `grep -r "from '@/types" apps/web/src/` returns nothing (or only in files that re-export for backward compat)
  - `grep -r "from '@/config" apps/web/src/` returns nothing
  - `grep -r "from '@/utils" apps/web/src/` returns nothing
  - `grep -r "from '@/api/queries" apps/web/src/` returns nothing
  - `ls apps/web/src/types/` returns "No such file or directory"
  - `ls apps/web/src/config/` returns "No such file or directory"
  - `ls apps/web/src/utils/` returns "No such file or directory"
  - `cd apps/web && npx vue-tsc --noEmit` passes
  - `bun run build` succeeds (both core and web)
  - `bun run dev` starts without errors
  </verify>
  <done>
  All web app imports point to @gnomad-cf/core/* for types, config, queries, filters, calculations, and templates. The old apps/web/src/types/, apps/web/src/config/, apps/web/src/utils/, and apps/web/src/api/queries/ directories are deleted. Web app builds and dev-serves correctly using core package. The composables, stores, and components remain in apps/web/ as Vue-specific code.
  </done>
</task>

</tasks>

<verification>
- `grep -r "@/" apps/web/src/` only shows imports for composables/, stores/, components/, api/client, App.vue, main.ts
- `grep -r "@gnomad-cf/core" apps/web/src/` shows correct core imports in composables, stores, and components
- `cd packages/core && bun run build` succeeds
- `bun run build` (root) builds core then web successfully
- `bun run dev` starts dev server, page loads correctly
- apps/web/src/ contains ONLY: api/ (client.ts, index.ts), composables/, stores/, components/, App.vue, main.ts, vite-env.d.ts
</verification>

<success_criteria>
The web app exclusively imports shared logic from @gnomad-cf/core/*. No duplicate files remain. The fetch-based core client exists for CLI use. Web composables are thin Vue wrappers calling core functions. Both build and dev workflows function correctly.
</success_criteria>

<output>
After completion, create `.planning/phases/25-monorepo-foundation/25-04-SUMMARY.md`
</output>
