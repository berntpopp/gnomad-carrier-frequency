---
phase: 25-monorepo-foundation
plan: 05
type: execute
wave: 5
depends_on: ["25-04"]
files_modified:
  - .github/workflows/deploy.yml
  - CLAUDE.md
  - .gitignore
  - package.json
autonomous: false

must_haves:
  truths:
    - "bun run build from root builds core then web without errors"
    - "The GitHub Actions deploy.yml builds core before web and deploys from apps/web/dist"
    - "bun run test runs vitest without errors"
    - "packages/core has zero imports from Vue, Pinia, or villus"
    - "CLAUDE.md reflects the new monorepo structure"
  artifacts:
    - path: ".github/workflows/deploy.yml"
      provides: "Monorepo-aware CI deploy pipeline"
      contains: "apps/web/dist"
    - path: "CLAUDE.md"
      provides: "Updated project documentation"
      contains: "packages/core"
  key_links:
    - from: ".github/workflows/deploy.yml"
      to: "packages/core"
      via: "bun run --filter @gnomad-cf/core build"
      pattern: "@gnomad-cf/core.*build"
    - from: ".github/workflows/deploy.yml"
      to: "apps/web/dist"
      via: "upload-pages-artifact path"
      pattern: "apps/web/dist"
---

<objective>
Update GitHub Actions CI/deploy for monorepo, clean up leftover files, update CLAUDE.md, and verify the full system end-to-end.

Purpose: Ensure the deployed app works identically to pre-v1.5. Update all meta-files (CI, docs, gitignore) to reflect the monorepo structure. This is the final plan that makes the phase complete and deployable.

Output: Working CI pipeline, clean repo with no stale files, updated documentation, human-verified build output.
</objective>

<execution_context>
@C:\Users\bernt\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\bernt\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-monorepo-foundation/25-CONTEXT.md
@.planning/phases/25-monorepo-foundation/25-RESEARCH.md
@.planning/phases/25-monorepo-foundation/25-04-SUMMARY.md

@.github/workflows/deploy.yml
@CLAUDE.md
@.gitignore
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update CI/deploy, cleanup, and documentation</name>
  <files>
    .github/workflows/deploy.yml
    CLAUDE.md
    .gitignore
    package.json
  </files>
  <action>
  **Step 1 — Update .github/workflows/deploy.yml:**
  Rewrite the deploy workflow for monorepo structure. Reference RESEARCH.md for the exact template. Key changes:
  - Install dependencies: `bun install --frozen-lockfile` (unchanged)
  - Lint: `bun run lint` (runs via filter in root package.json)
  - Type check: `bun run typecheck` (tsc --build with project references)
  - Build core: `bun run --filter @gnomad-cf/core build`
  - Build web: `bun run --filter gnomad-cf-web build`
  - Build docs: `bun run --filter gnomad-cf-web docs:build`
  - Merge docs: `cp -r apps/web/docs/.vitepress/dist apps/web/dist/docs`
  - Upload artifact path: `'./apps/web/dist'` (was `'./dist'`)

  **Step 2 — Update .gitignore:**
  Ensure these entries exist:
  - `node_modules/` (covers all workspace node_modules)
  - `dist/` (covers packages/core/dist/ and apps/web/dist/)
  - `bun.lockb` (prevent binary lockfile from being committed)
  - `*.tsbuildinfo` (TypeScript build cache)
  - Keep existing entries for `.vitepress/cache`, `.vitepress/dist`, etc.

  Remove any entries that reference old paths (e.g., bare `dist` without `/` that might not match nested).

  **Step 3 — Update CLAUDE.md:**
  Rewrite the project overview and architecture sections to reflect the monorepo structure:
  - Update "Architecture / Layer Structure" to show the `packages/core/` and `apps/web/` layout
  - Update "Commands" to show root-level `bun run dev`, `bun run build`, `bun run test`
  - Update "Data Flow" to mention that core logic lives in @gnomad-cf/core
  - Update "Tech Stack" to mention tsdown for core package
  - Add a "Monorepo Structure" section explaining packages/core and apps/web
  - Keep the gnomAD Integration and Clinical Text System sections — just note imports come from @gnomad-cf/core

  **Step 4 — Clean up any stale root files:**
  Check for files that should have been moved but weren't:
  ```bash
  ls *.ts *.json *.js *.html 2>/dev/null
  ```
  Root should only have: package.json, tsconfig.json, vitest.config.ts, bun.lock, .gitignore, CLAUDE.md, README.md, and possibly .env files. If vite.config.ts, eslint.config.js, index.html, tsconfig.app.json, tsconfig.node.json still exist at root, delete them (they were moved to apps/web/).

  Also check for stale scripts in root package.json that reference old paths.

  **Step 5 — Run final verification battery:**
  ```bash
  # Clean install
  rm -rf node_modules packages/*/node_modules apps/*/node_modules
  bun install

  # Typecheck
  bun run typecheck

  # Lint
  bun run lint

  # Build
  bun run build

  # Test
  bun run test

  # Check core isolation
  grep -r "from 'vue'" packages/core/src/ || echo "PASS: no vue imports"
  grep -r "from 'villus'" packages/core/src/ || echo "PASS: no villus imports"
  grep -r "from 'pinia'" packages/core/src/ || echo "PASS: no pinia imports"
  ```
  All must pass.

  **Step 6 — Verify build output matches expectations:**
  - `ls apps/web/dist/index.html` exists
  - `ls apps/web/dist/assets/` contains JS and CSS bundles
  - `ls packages/core/dist/index.js` exists
  - `ls packages/core/dist/index.d.ts` exists
  </action>
  <verify>
  - `bun install` succeeds (clean install)
  - `bun run typecheck` passes
  - `bun run lint` passes
  - `bun run build` builds core then web
  - `bun run test` runs vitest (0 tests OK)
  - `apps/web/dist/index.html` exists
  - `packages/core/dist/index.js` exists
  - No Vue/Pinia/villus imports in packages/core/src/
  - `.github/workflows/deploy.yml` references `apps/web/dist`
  - CLAUDE.md mentions monorepo structure
  </verify>
  <done>
  CI deploy pipeline updated for monorepo. All stale files cleaned up. CLAUDE.md reflects new architecture. Full verification battery passes (install, typecheck, lint, build, test).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
  Complete monorepo restructure: packages/core (@gnomad-cf/core) with all shared logic, apps/web with Vue app importing from core, updated CI/deploy, and clean repository structure.
  </what-built>
  <how-to-verify>
  1. Run `bun run dev` at repo root — verify the dev server starts and the app loads at http://localhost:5173
  2. In the running app, search for "CFTR" in the gene search field
  3. Complete the wizard flow through to carrier frequency results — verify numbers appear correctly
  4. Check the browser console for any errors (especially module resolution errors)
  5. Run `bun run build` — verify both core and web build without errors
  6. Run `bun run test` — verify vitest runs (0 tests is fine, should not error)
  7. Verify apps/web/dist/index.html exists after build
  8. Spot-check a composable file (e.g., apps/web/src/composables/useCarrierFrequency.ts) to confirm it imports from @gnomad-cf/core/* rather than @/ paths
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
- CI deploy.yml uses correct monorepo paths (apps/web/dist)
- Clean install + build succeeds
- All root scripts work (dev, build, test, lint, typecheck)
- packages/core has zero Vue/Pinia/villus dependencies
- CLAUDE.md is current with monorepo structure
- Human verified: app loads and CFTR wizard works correctly
</verification>

<success_criteria>
The repository is a working bun workspaces monorepo. packages/core (@gnomad-cf/core) contains all shared types, config, queries, filters, calculations, templates, and a fetch-based client. apps/web imports exclusively from @gnomad-cf/core/* for shared logic. CI deploys correctly from the new structure. Human has verified the app works end-to-end. The phase goal is met: "The repository is restructured as a bun workspaces monorepo and the web app builds and deploys identically from the new structure."
</success_criteria>

<output>
After completion, create `.planning/phases/25-monorepo-foundation/25-05-SUMMARY.md`
</output>
