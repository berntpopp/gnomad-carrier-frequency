---
phase: 07-seo-accessibility
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
  - public/og-image.png
autonomous: true

must_haves:
  truths:
    - "Search engines see descriptive meta description in page source"
    - "Social previews show custom OG image and title"
    - "Structured data validates as WebApplication schema"
  artifacts:
    - path: "index.html"
      provides: "SEO meta tags, OG tags, structured data"
      contains: "og:image"
    - path: "public/og-image.png"
      provides: "Social preview image"
      min_size: 10000
  key_links:
    - from: "index.html"
      to: "public/og-image.png"
      via: "og:image meta tag"
      pattern: "og:image.*og-image.png"
---

<objective>
Add SEO meta tags, Open Graph tags, Twitter cards, and JSON-LD structured data to index.html, plus create an OG preview image.

Purpose: Improve discoverability and professional appearance when shared on social media or indexed by search engines. Addresses SEO-01 (meta description), contributes to INFRA-07 (Lighthouse SEO >= 95).

Output: index.html with complete SEO markup, og-image.png in public folder
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-seo-accessibility/07-RESEARCH.md
@.planning/phases/07-seo-accessibility/07-CONTEXT.md
@index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add meta description and OG tags to index.html</name>
  <files>index.html</files>
  <action>
Update index.html with:

1. Meta description (clinical/professional tone for genetic counselors):
```html
<meta name="description" content="Calculate carrier frequency and recurrence risk for autosomal recessive conditions. Uses gnomAD population data to generate clinical documentation for genetic counseling." />
```

2. Open Graph tags:
- og:type = "website"
- og:title = "gnomAD Carrier Frequency Calculator"
- og:description = same as meta description
- og:image = "./og-image.png" (relative for GitHub Pages)
- og:image:width = "1200"
- og:image:height = "630"
- og:url = "https://berntpopp.github.io/gnomad-carrier-frequency/"

3. Twitter Card tags:
- twitter:card = "summary_large_image"
- twitter:title = "gnomAD Carrier Frequency Calculator"
- twitter:description = same as meta description
- twitter:image = "./og-image.png"

Place all meta tags in head section after existing viewport meta tag.
  </action>
  <verify>View index.html source and confirm all meta tags present with correct values</verify>
  <done>Meta description, OG tags, and Twitter cards present in index.html</done>
</task>

<task type="auto">
  <name>Task 2: Add JSON-LD structured data</name>
  <files>index.html</files>
  <action>
Add script tag with JSON-LD structured data inside head section:

```html
<script type="application/ld+json">
{
  "@context": "https://schema.org",
  "@type": "WebApplication",
  "name": "gnomAD Carrier Frequency Calculator",
  "alternateName": "gCFCalc",
  "url": "https://berntpopp.github.io/gnomad-carrier-frequency/",
  "description": "Calculate carrier frequency and recurrence risk for autosomal recessive conditions using gnomAD population data",
  "applicationCategory": "HealthApplication",
  "operatingSystem": "All",
  "browserRequirements": "Requires JavaScript",
  "author": {
    "@type": "Person",
    "name": "Bernt Popp"
  }
}
</script>
```

Place after the meta tags, before closing head tag.
  </action>
  <verify>Validate JSON-LD using Google Rich Results Test (https://search.google.com/test/rich-results) or view source</verify>
  <done>WebApplication structured data present in index.html</done>
</task>

<task type="auto">
  <name>Task 3: Create OG preview image</name>
  <files>public/og-image.png</files>
  <action>
Create a simple 1200x630 PNG image for social previews.

Design approach (since Claude cannot generate images directly):
1. Create an SVG file first that can be converted to PNG, OR
2. Use a simple solid color background with text

For simplicity, create an SVG that matches the app branding:
- Background: #FAFAFA (light theme background)
- Centered text: "gCFCalc" in primary color #a09588
- Subtitle: "gnomAD Carrier Frequency Calculator"

Create public/og-image.svg first:
```svg
<svg xmlns="http://www.w3.org/2000/svg" width="1200" height="630" viewBox="0 0 1200 630">
  <rect width="1200" height="630" fill="#FAFAFA"/>
  <text x="600" y="280" font-family="system-ui, -apple-system, sans-serif" font-size="120" font-weight="700" fill="#a09588" text-anchor="middle">gCFCalc</text>
  <text x="600" y="380" font-family="system-ui, -apple-system, sans-serif" font-size="36" fill="#424242" text-anchor="middle">gnomAD Carrier Frequency Calculator</text>
</svg>
```

Then convert to PNG using a command-line tool if available, or note that the SVG can be used directly (update index.html og:image to point to .svg).

Alternative: If PNG conversion not available, update og:image tags to use og-image.svg instead. SVG is valid for OG images in modern platforms.
  </action>
  <verify>File exists at public/og-image.svg or public/og-image.png, file size > 0</verify>
  <done>OG preview image created and referenced in index.html</done>
</task>

</tasks>

<verification>
1. `cat index.html | grep -E "(description|og:|twitter:)"` shows all meta tags
2. `cat index.html | grep "application/ld+json"` confirms structured data present
3. `ls -la public/og-image.*` confirms preview image exists
4. Build succeeds: `bun run build`
</verification>

<success_criteria>
- index.html contains meta description with clinical tone
- index.html contains OG tags with all required properties
- index.html contains Twitter card tags
- index.html contains valid JSON-LD WebApplication schema
- OG preview image exists in public folder
- Application builds without errors
</success_criteria>

<output>
After completion, create `.planning/phases/07-seo-accessibility/07-01-SUMMARY.md`
</output>
