---
phase: 14-mobile-optimization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/SettingsDialog.vue
  - src/components/LogViewerPanel.vue
  - src/components/wizard/WizardStepper.vue
autonomous: true

must_haves:
  truths:
    - "Settings dialog displays fullscreen on mobile viewports"
    - "Log viewer panel fits within mobile viewport without overflow"
    - "Wizard stepper displays all 4 steps without horizontal scroll on mobile"
    - "User can navigate between tabs in settings on mobile"
  artifacts:
    - path: "src/components/SettingsDialog.vue"
      provides: "Fullscreen dialog on mobile"
      contains: "useDisplay"
    - path: "src/components/LogViewerPanel.vue"
      provides: "Responsive width"
      contains: "smAndDown"
    - path: "src/components/wizard/WizardStepper.vue"
      provides: "Mobile-optimized stepper"
      contains: "alt-labels"
  key_links:
    - from: "src/components/SettingsDialog.vue"
      to: "vuetify useDisplay"
      via: "composable import"
      pattern: "import.*useDisplay.*from.*vuetify"
---

<objective>
Make core dialogs and navigation components responsive for mobile viewports.

Purpose: Addresses MOB-02, MOB-04, MOB-07 - dialogs must fit viewport and navigation must work on mobile.
Output: SettingsDialog uses fullscreen on mobile, LogViewerPanel responsive width, WizardStepper with alt-labels.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-mobile-optimization/14-RESEARCH.md
@src/components/SettingsDialog.vue
@src/components/LogViewerPanel.vue
@src/components/wizard/WizardStepper.vue
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make SettingsDialog responsive with fullscreen on mobile</name>
  <files>src/components/SettingsDialog.vue</files>
  <action>
    Import `useDisplay` from 'vuetify' and use `smAndDown` for responsive behavior:

    1. Add import: `import { useDisplay } from 'vuetify'`
    2. In script setup, destructure: `const { smAndDown } = useDisplay()`
    3. Update v-dialog props:
       - Add `:fullscreen="smAndDown"`
       - Change `:max-width` to use conditional: `:max-width="smAndDown ? undefined : (activeTab === 'templates' ? 900 : 600)"`
    4. In the templates tab, the two-column layout (d-flex flex-column flex-md-row) is already responsive, but verify the VariablePicker min-width works on mobile - if needed, add responsive width.
    5. Ensure v-tabs on mobile don't overflow - they should already scroll, but verify.

    The goal is fullscreen dialog on xs/sm screens (< 960px per Vuetify defaults).
  </action>
  <verify>
    - Open app in browser dev tools with mobile viewport (375px width)
    - Open Settings dialog
    - Dialog should fill entire screen (fullscreen mode)
    - All tabs should be navigable
    - Content should not overflow horizontally
  </verify>
  <done>Settings dialog displays fullscreen on mobile, all tabs accessible, no horizontal overflow</done>
</task>

<task type="auto">
  <name>Task 2: Make LogViewerPanel responsive width</name>
  <files>src/components/LogViewerPanel.vue</files>
  <action>
    Update the v-navigation-drawer to use responsive width:

    1. Add import: `import { useDisplay } from 'vuetify'`
    2. In script setup: `const { smAndDown } = useDisplay()`
    3. Change the width prop from fixed 450 to responsive:
       `:width="smAndDown ? '100%' : 450"`

    This ensures the log panel fills the screen on mobile but stays at 450px on larger screens.
  </action>
  <verify>
    - Open app in browser dev tools with mobile viewport
    - Click log viewer icon in footer
    - Panel should fill entire width on mobile
    - On desktop (> 960px), panel should be 450px wide
  </verify>
  <done>Log viewer panel uses 100% width on mobile, 450px on larger screens</done>
</task>

<task type="auto">
  <name>Task 3: Make WizardStepper mobile-friendly with alt-labels</name>
  <files>src/components/wizard/WizardStepper.vue</files>
  <action>
    Add alt-labels on mobile to stack step labels below icons:

    1. Add import: `import { useDisplay } from 'vuetify'`
    2. In script setup: `const { smAndDown } = useDisplay()`
    3. Add `:alt-labels="smAndDown"` to the v-stepper component

    This makes step labels display below their icons on mobile, preventing horizontal overflow.

    Note: The current step subtitles may still be long. If testing reveals issues, consider:
    - Shortening subtitles on mobile via computed property
    - Or hiding subtitles entirely on xs screens with `v-if="!smAndDown"`

    Start with just alt-labels and verify if further changes needed.
  </action>
  <verify>
    - Open app on mobile viewport (375px width)
    - Wizard stepper should show all 4 steps
    - Step labels should appear below icons (stacked, not beside)
    - No horizontal scrollbar on the stepper
    - All steps should be visible and tappable
  </verify>
  <done>Wizard stepper uses alt-labels on mobile, all 4 steps visible without horizontal scroll</done>
</task>

</tasks>

<verification>
After completing all tasks:
1. Test on mobile viewport (375px) - Dev tools responsive mode
2. Settings dialog opens fullscreen
3. Log viewer panel fills screen width
4. Wizard stepper shows all steps without horizontal scroll
5. `bun run build` succeeds
6. `bun run lint` passes
</verification>

<success_criteria>
- MOB-02: Wizard steps display correctly on mobile (alt-labels enabled)
- MOB-04: Settings dialog works on mobile (fullscreen mode)
- MOB-07: Modals and dialogs fit within mobile viewport
- All 3 components use useDisplay for responsive behavior
- No build or lint errors
</success_criteria>

<output>
After completion, create `.planning/phases/14-mobile-optimization/14-01-SUMMARY.md`
</output>
