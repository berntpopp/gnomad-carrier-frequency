---
phase: 34-quality-flags-source-breakdown
plan: 03
type: execute
wave: 3
depends_on: ["34-02"]
files_modified:
  - apps/web/src/components/SettingsDialog.vue
  - apps/web/src/components/FilterPanel.vue
autonomous: true

must_haves:
  truths:
    - "Settings dialog has a 4th Quality tab with grouped card sections for each flag type"
    - "High AF card has enable toggle and slider for threshold (default 5%)"
    - "High Hom card has enable toggle, method selector (HWE-relative/absolute), and multiplier/threshold control"
    - "gnomAD Filtered and Genomes Only cards have enable toggle only"
    - "Quality tab has Reset to Defaults button"
    - "FilterPanel has per-analysis quality exclusion toggles"
    - "FilterPanel shows active quality exclusion count when exclusions are enabled"
    - "Quality settings changes in SettingsDialog persist to localStorage"
  artifacts:
    - path: "apps/web/src/components/SettingsDialog.vue"
      provides: "Quality tab with flag threshold configuration"
    - path: "apps/web/src/components/FilterPanel.vue"
      provides: "Per-analysis quality exclusion toggle section"
  key_links:
    - from: "apps/web/src/components/SettingsDialog.vue"
      to: "apps/web/src/stores/useQualityStore.ts"
      via: "useQualityStore() for v-model binding"
      pattern: "useQualityStore"
    - from: "apps/web/src/components/FilterPanel.vue"
      to: "parent component"
      via: "emit update:qualityExclusionConfig"
      pattern: "update:qualityExclusionConfig"
---

<objective>
Add the Quality tab to SettingsDialog for configuring global quality flag thresholds, and add per-analysis quality exclusion toggles to FilterPanel.

Purpose: Users need two levels of quality flag control: (1) global defaults in Settings that persist across sessions, and (2) per-analysis overrides in the FilterPanel that apply only to the current calculation. This matches the existing pattern where filter defaults live in Settings and per-analysis overrides live in FilterPanel.

Output: SettingsDialog with 4th Quality tab, FilterPanel with quality exclusion toggle section.
</objective>

<execution_context>
@C:\Users\bernt\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\bernt\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-quality-flags-source-breakdown/34-CONTEXT.md
@.planning/phases/34-quality-flags-source-breakdown/34-RESEARCH.md
@.planning/phases/34-quality-flags-source-breakdown/34-01-SUMMARY.md
@.planning/phases/34-quality-flags-source-breakdown/34-02-SUMMARY.md
@apps/web/src/components/SettingsDialog.vue
@apps/web/src/components/FilterPanel.vue
@apps/web/src/stores/useQualityStore.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Quality tab to SettingsDialog</name>
  <files>apps/web/src/components/SettingsDialog.vue</files>
  <action>
    Modify `SettingsDialog.vue` to add a 4th "Quality" tab. Follow the existing tab pattern exactly.

    1. Import the quality store:
       ```typescript
       import { useQualityStore } from '@/stores/useQualityStore';
       ```
       Initialize: `const qualityStore = useQualityStore();`

    2. Add the new tab to the `v-tabs` component (after "Templates" tab):
       ```html
       <v-tab value="quality" data-testid="settings-tab-quality">Quality</v-tab>
       ```

    3. Update `dialogMaxWidth` computed -- Quality tab at 600px is fine (same as General/Filters). No change needed since the default is 600 for non-templates tabs.

    4. Add `v-tabs-window-item value="quality"` inside the `v-tabs-window`, after the templates tab-window-item:

       Content structure -- explanatory text followed by grouped card sections:

       ```html
       <p class="text-body-2 text-medium-emphasis mb-4">
         Flag variants that may need review. Flagged variants can be excluded
         per-analysis in the filter panel.
       </p>
       ```

       **Card 1: High AF (BA1)**
       ```html
       <v-card variant="outlined" class="mb-4">
         <v-card-title class="text-subtitle-1">
           <v-icon start size="small" color="error">mdi-alert-circle</v-icon>
           High Allele Frequency (BA1)
         </v-card-title>
         <v-card-text>
           <v-switch
             v-model="qualityStore.defaults.highAfEnabled"
             color="error"
             label="Flag variants exceeding AF threshold"
             density="compact"
             hide-details
             class="mb-3"
           />
           <v-slider
             v-model="highAfPercent"
             :disabled="!qualityStore.defaults.highAfEnabled"
             :min="1"
             :max="20"
             :step="0.5"
             label="AF Threshold %"
             thumb-label
             color="error"
             density="compact"
           >
             <template #append>
               <span class="text-body-2">{{ highAfPercent }}%</span>
             </template>
           </v-slider>
           <div class="text-caption text-medium-emphasis">
             ACMG BA1: Variants with allele frequency above this threshold
             in any population may be too common for a rare recessive disease.
           </div>
         </v-card-text>
       </v-card>
       ```

       Create a computed for the slider (0-100% integer display vs 0-1 stored value):
       ```typescript
       const highAfPercent = computed({
         get: () => qualityStore.defaults.highAfThreshold * 100,
         set: (v: number) => qualityStore.setDefaults({ highAfThreshold: v / 100 }),
       });
       ```

       **Card 2: High Homozygote Count**
       ```html
       <v-card variant="outlined" class="mb-4">
         <v-card-title class="text-subtitle-1">
           <v-icon start size="small" color="orange">mdi-alert</v-icon>
           High Homozygote Count
         </v-card-title>
         <v-card-text>
           <v-switch
             v-model="qualityStore.defaults.highHomEnabled"
             color="orange"
             label="Flag variants with unexpectedly high homozygotes"
             density="compact"
             hide-details
             class="mb-3"
           />
           <v-btn-toggle
             v-model="qualityStore.defaults.highHomMethod"
             :disabled="!qualityStore.defaults.highHomEnabled"
             mandatory
             density="compact"
             color="orange"
             variant="outlined"
             class="mb-3"
           >
             <v-btn value="hwe_relative" size="small">HWE-Relative</v-btn>
             <v-btn value="absolute" size="small">Absolute</v-btn>
           </v-btn-toggle>

           <!-- HWE-relative multiplier -->
           <v-slider
             v-if="qualityStore.defaults.highHomMethod === 'hwe_relative'"
             v-model="qualityStore.defaults.highHomHWEMultiplier"
             :disabled="!qualityStore.defaults.highHomEnabled"
             :min="2"
             :max="20"
             :step="1"
             label="HWE Multiplier"
             thumb-label
             color="orange"
             density="compact"
           >
             <template #append>
               <span class="text-body-2">{{ qualityStore.defaults.highHomHWEMultiplier }}x</span>
             </template>
           </v-slider>

           <!-- Absolute threshold -->
           <v-slider
             v-else
             v-model="qualityStore.defaults.highHomAbsoluteThreshold"
             :disabled="!qualityStore.defaults.highHomEnabled"
             :min="1"
             :max="50"
             :step="1"
             label="Max Homozygotes"
             thumb-label
             color="orange"
             density="compact"
           >
             <template #append>
               <span class="text-body-2">{{ qualityStore.defaults.highHomAbsoluteThreshold }}</span>
             </template>
           </v-slider>

           <div class="text-caption text-medium-emphasis">
             Flag when observed homozygotes exceed expected count.
             HWE-Relative flags if observed > multiplier x HWE-expected (AF^2 x AN).
           </div>
         </v-card-text>
       </v-card>
       ```

       **Card 3: gnomAD Filtered**
       ```html
       <v-card variant="outlined" class="mb-4">
         <v-card-title class="text-subtitle-1">
           <v-icon start size="small" color="amber">mdi-filter-remove</v-icon>
           gnomAD Quality Filters
         </v-card-title>
         <v-card-text>
           <v-switch
             v-model="qualityStore.defaults.gnomadFilteredEnabled"
             color="amber"
             label="Flag variants that failed gnomAD QC filters"
             density="compact"
             hide-details
           />
           <div class="text-caption text-medium-emphasis mt-2">
             Variants flagged by gnomAD's internal quality filters (e.g., random forest,
             allele balance, inbreeding coefficient) may have lower reliability.
           </div>
         </v-card-text>
       </v-card>
       ```

       **Card 4: Genomes Only**
       ```html
       <v-card variant="outlined" class="mb-4">
         <v-card-title class="text-subtitle-1">
           <v-icon start size="small" color="blue-grey">mdi-dna</v-icon>
           Genomes Only
         </v-card-title>
         <v-card-text>
           <v-switch
             v-model="qualityStore.defaults.genomesOnlyEnabled"
             color="blue-grey"
             label="Flag variants observed only in genome data"
             density="compact"
             hide-details
           />
           <div class="text-caption text-medium-emphasis mt-2">
             Variants without exome coverage may have different quality characteristics
             compared to exome-sequenced variants.
           </div>
         </v-card-text>
       </v-card>
       ```

       **Reset button (at bottom):**
       ```html
       <v-divider class="my-4" />
       <v-btn
         variant="outlined"
         color="warning"
         size="small"
         prepend-icon="mdi-restore"
         @click="qualityStore.resetToFactoryDefaults()"
       >
         Reset to Factory Defaults
       </v-btn>
       ```
  </action>
  <verify>
    Run `cd /c/development/gnomad-carrier-frequency && bun run typecheck` -- passes.
    Run `cd /c/development/gnomad-carrier-frequency && bun run dev` and manually verify: Settings dialog opens, Quality tab renders with 4 cards, sliders work, toggles work, Reset button resets all values.
  </verify>
  <done>
    SettingsDialog has a 4th Quality tab with card-style sections for each flag type. High AF has slider + toggle, High Hom has method selector + conditional slider, gnomAD Filtered and Genomes Only are toggle-only. All settings persist via useQualityStore.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add quality exclusion toggles to FilterPanel</name>
  <files>apps/web/src/components/FilterPanel.vue</files>
  <action>
    Modify `FilterPanel.vue` to add per-analysis quality flag exclusion controls. These appear alongside the existing Calculation Settings section (HWE Formula, Homozygote Exclusion, Penetrance).

    1. Add new props for quality exclusion:
       ```typescript
       qualityExclusionConfig: QualityExclusionConfig;
       qualityExcludedCount?: number;
       flaggedVariantCount?: number;
       ```
       Import `QualityExclusionConfig` from `@gnomad-cf/core/types`.

    2. Add new emit:
       ```typescript
       'update:qualityExclusionConfig': [value: QualityExclusionConfig];
       ```

    3. Create helper function:
       ```typescript
       function updateQualityExclusion<K extends keyof QualityExclusionConfig>(
         key: K,
         value: QualityExclusionConfig[K],
       ) {
         emit('update:qualityExclusionConfig', {
           ...props.qualityExclusionConfig,
           [key]: value,
         });
       }
       ```

    4. Add a new section AFTER the Calculation Settings row (after the Penetrance slider col). Add a divider first:

       ```html
       <!-- Quality Flag Exclusions section -->
       <v-col cols="12">
         <v-divider class="mb-3" />
         <div class="text-subtitle-2 mb-2">Quality Flag Exclusions</div>
         <div class="text-caption text-medium-emphasis mb-3">
           Exclude flagged variants from the carrier frequency calculation.
           {{ flaggedCount }} of {{ variantCount }} variant(s) flagged.
         </div>
       </v-col>

       <v-col cols="12" md="6">
         <v-switch
           :model-value="qualityExclusionConfig.excludeHighAf"
           color="error"
           label="Exclude High AF"
           :density="smAndDown ? 'default' : 'compact'"
           hide-details
           @update:model-value="updateQualityExclusion('excludeHighAf', $event)"
         />
       </v-col>

       <v-col cols="12" md="6">
         <v-switch
           :model-value="qualityExclusionConfig.excludeHighHom"
           color="orange"
           label="Exclude High Hom"
           :density="smAndDown ? 'default' : 'compact'"
           hide-details
           @update:model-value="updateQualityExclusion('excludeHighHom', $event)"
         />
       </v-col>

       <v-col cols="12" md="6">
         <v-switch
           :model-value="qualityExclusionConfig.excludeGnomadFiltered"
           color="amber"
           label="Exclude gnomAD Filtered"
           :density="smAndDown ? 'default' : 'compact'"
           hide-details
           @update:model-value="updateQualityExclusion('excludeGnomadFiltered', $event)"
         />
       </v-col>

       <v-col cols="12" md="6">
         <v-switch
           :model-value="qualityExclusionConfig.excludeGenomesOnly"
           color="blue-grey"
           label="Exclude Genomes Only"
           :density="smAndDown ? 'default' : 'compact'"
           hide-details
           @update:model-value="updateQualityExclusion('excludeGenomesOnly', $event)"
         />
       </v-col>
       ```

    5. Add quality exclusion active alert (shown when any quality exclusions are active):
       ```html
       <v-col v-if="activeQualityExclusionCount > 0" cols="12">
         <v-alert type="info" variant="tonal" density="compact">
           Excluding {{ qualityExcludedDisplay }} variant(s) by quality flags.
         </v-alert>
       </v-col>
       ```

       Create computeds:
       ```typescript
       const flaggedCount = computed(() => props.flaggedVariantCount ?? 0);
       const qualityExcludedDisplay = computed(() => props.qualityExcludedCount ?? 0);
       const activeQualityExclusionCount = computed(() => {
         const c = props.qualityExclusionConfig;
         return [c.excludeHighAf, c.excludeHighHom, c.excludeGnomadFiltered, c.excludeGenomesOnly]
           .filter(Boolean).length;
       });
       ```

    6. Update the summary line at the bottom of FilterPanel. The existing line shows `{{ variantCount }} qualifying variant(s)`. Enhance to include quality-excluded count context when active:
       Update the existing `<strong>{{ variantCount }}</strong> qualifying variant(s)` text to also show quality exclusion info if active:
       ```html
       <div class="text-body-2">
         <strong>{{ variantCount }}</strong> qualifying variant(s)
         <span v-if="qualityExcludedDisplay > 0" class="text-warning">
           ({{ qualityExcludedDisplay }} quality-excluded)
         </span>
       </div>
       ```

    7. Wire the new props from the parent component. The parent is `StepResults.vue` (or wherever FilterPanel is used). This wiring will happen in Plan 04 when StepResults is updated. For now, ensure the props have safe defaults and the component works even if the new props are not yet passed.
       Add default values via computed:
       ```typescript
       const qualityExclusionConfig = computed(() => props.qualityExclusionConfig ?? {
         excludeHighAf: false, excludeHighHom: false,
         excludeGnomadFiltered: false, excludeGenomesOnly: false,
       });
       ```
       Wait -- since this is a required prop, the parent must pass it. But during the transition between plans, the parent won't pass it yet. Make the prop optional with a `?` and use the computed fallback pattern. The Plan 04 wiring will make it required.

       Actually, to avoid breaking existing usage: make `qualityExclusionConfig` optional in the props with `?`:
       ```typescript
       qualityExclusionConfig?: QualityExclusionConfig;
       ```
       And guard the template: only show the quality exclusion section when the prop is provided:
       ```html
       <template v-if="qualityExclusionConfig">
         <!-- quality exclusion toggles -->
       </template>
       ```
  </action>
  <verify>
    Run `cd /c/development/gnomad-carrier-frequency && bun run typecheck` -- passes.
    Run `cd /c/development/gnomad-carrier-frequency && bun run build` -- builds.
    Run `cd /c/development/gnomad-carrier-frequency && bun run test -- --run` -- all tests pass.
  </verify>
  <done>
    FilterPanel has quality exclusion toggles (4 switches, one per flag type) that appear when qualityExclusionConfig prop is provided. Colors match the flag severity scheme. Active exclusion count displayed. The section is backwards-compatible (hidden when prop not passed).
  </done>
</task>

</tasks>

<verification>
- `bun run typecheck` passes
- `bun run build` succeeds
- `bun run test -- --run` passes
- Settings > Quality tab renders with 4 card sections
- FilterPanel shows quality exclusion toggles when prop provided
- Quality store persists to localStorage
</verification>

<success_criteria>
- QUAL-05: Quality flag thresholds configurable in settings (High AF slider with 5% default)
- QUAL-07: Per-flag-type exclusion toggles in FilterPanel
- QUAL-08: Explanatory text on each flag card in Settings
- Settings Quality tab has Reset to Defaults button
- FilterPanel exclusion section is backwards-compatible (hidden if prop not passed)
</success_criteria>

<output>
After completion, create `.planning/phases/34-quality-flags-source-breakdown/34-03-SUMMARY.md`
</output>
